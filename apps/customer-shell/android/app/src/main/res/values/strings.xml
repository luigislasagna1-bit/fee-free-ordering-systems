<?xml version='1.0' encoding='utf-8'?>
<!-- TEMPLATE — deliberately empty of tenant identity. app_name,
     title_activity_main, package_name and custom_url_scheme are all injected
     per build via gradle resValue (see app/build.gradle). A tenant string
     committed here would leak into every other restaurant's build. -->
<resources>
</resources>
