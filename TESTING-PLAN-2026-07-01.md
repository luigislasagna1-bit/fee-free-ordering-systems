# Test Plan — 20h session review (commits cc4c98f0..012a0144 → export fix daf69539)

_Generated by the ultracode adversarial review (19 agents). All work was validated by `tsc` + logic; this plan covers everything still needing on-device verification before moving to reseller reports._

## Review verdict

- **No critical / high / medium issues found.** Adversarial review across 6 dimensions (timezone, coupon/money, banner-UI, dead-code/i18n, scale/security, completeness).
- Confirmed findings (all LOW/NIT):
  - **[LOW]** `conn-dst-day-bucket` — The per-day uptime bucket derives its END from a fixed 24h constant (dayEnd = dayStart + DAY_MS) instead of the true next local midnight. dayStart is correctly 
  - **[LOW]** `collapse-seed-desktop-flash` — OrderingPageClient is a 'use client' component that is server-rendered (imported directly in page.tsx line 469, not ssr:false). collapsedCats initializes to an 
  - **[NIT]** `isMobile-dep-unused-in-body` — isMobile was removed from the effect BODY but kept in the deps array. This is not a lint error (it is 'used' by being referenced in the deps list, and exhaustiv
  - **[NIT]** `stale-script-comment` — A run-once migration script (already executed 2026-06-30) still references the now-deleted component by name in its header comment. It is only a documentation c
  - **[LOW]** `tz-connectivity-export-divergence` — Connectivity page renders device First/Last Seen in restaurant tz but the export route uses bare toLocaleString (server UTC), so page and CSV/PDF disagree.
  - **1 finding rejected** (orphaned assign-to-customer route → intentionally kept; live "give a customer an offer" runs through GiveVipSpecial → a different route, so nothing is unreachable).

**Fixed after review:** the report EXPORT routes (List-Orders, List-Clients, Connectivity) were still emitting UTC dates while the pages now show local tz — fixed in commit `daf69539` so exports match the pages.

**Accepted low-priority (not fixed — flagged):** connectivity DST-day bucket uses a flat 24h (twice-a-year ~4% drift on a transition day); desktop collapse briefly flashes expanded before collapsing (same as mobile always did); one nit comment in an already-run migration script.

## Completeness critic

_Readiness: NOT exhaustive enough to sign off as-is. The assembled plan is thorough on the timezone work (the 6 surfaces are well covered, including positive/negative offset, cross-restaurant, date-only vs date+time, and the connectivity bucketing), but the diff cc4c98f0..012a0144 contains THREE other behavior changes with zero coverage, one of which is payments-critical: (1) the coupon min-order fix — a checkout hot-path change that must prove both the empty-cart acceptance AND that promo-engine still blocks a below-minimum discount at order time (CPN-P0-01/02); (2) the category dish-count pill, which is_

---

## A. TIMEZONE

_Restaurant-timezone rendering of order/customer/device dates across marketplace account pages and admin Reports (List Orders, List Clients, Customer detail, Connectivity uptime)_

**Setup:** PRECONDITIONS / DATA / CONFIG to stage before testing. Do these once; every case below reuses them.

A. TWO test restaurants with deliberately extreme timezones, so a UTC-vs-local bug is visually obvious:
   - "West store" — set timezone to America/Los_Angeles (Pacific, UTC-7/-8). A 11pm-Pacific order is the NEXT UTC day, so a UTC bug shows tomorrow's date.
   - "East store" — set timezone to Australia/Sydney (UTC+10/+11). A 12:30am-Sydney order is the PREVIOUS UTC day, so a UTC bug shows yesterday's date. (This is the positive-offset case the plan requires.)
   Set each via Admin > Profile (/admin/profile): find the "Timezone / Time zone" field, choose the IANA zone, Save. Confirm it saved by reloading the page.

B. SERVER CLOCK NOTE (why this matters): these pages are server-rendered. Before the change a naked toLocale* used the server's UTC clock. To make the bug reproducible even when your own laptop is in the same zone as the store, the server (Vercel) runs in UTC — so any late-evening order that crosses midnight-UTC is the trap. You do NOT need to change your laptop clock; the server is already UTC.

C. Seed a LATE-EVENING order that crosses the UTC day boundary. Two ways:
   - Easiest for a non-dev: place a REAL test order from the customer /order/<slug> page during the window where the store's LOCAL time is late evening but UTC has already rolled to the next day (Pacific: place it after 4:00pm your local afternoon if you are in Pacific — 11pm isn't required, any local time at/after 5pm Pacific is already tomorrow in UTC in winter). If you can't wait for the right wall-clock window, ask the dev to run a seed script setting the order's createdAt to a fixed instant.
   - Deterministic instant to request from the dev (copy-paste): create one order per store with createdAt = 2026-07-01T06:30:00Z (6:30am UTC). In America/Los_Angeles that instant is 2026-06-30 11:30pm (PREVIOUS calendar day). In Australia/Sydney that instant is 2026-07-01 4:30pm (SAME day). Also create createdAt = 2026-07-01T13:30:00Z: in Sydney that is 2026-07-01 11:30pm; in Los_Angeles it is 2026-07-01 6:30am. These two instants let one seeded order prove correctness on BOTH stores at once.

D. A DST-boundary instant (US spring-forward 2026 = Mar 8, 02:00 local; fall-back Nov 1 2026, 02:00 local). Ask the dev to seed an order at createdAt = 2026-03-08T09:30:00Z and another at 2026-11-01T08:30:00Z on the West (Los_Angeles) store, plus set that store's Connectivity/device lastSeenAt near those instants (see connectivity case). This proves the offset is computed per-instant (DST-aware), not with a fixed offset.

E. Sign in as the customer account that placed the orders (for /account and /account/orders) AND as the restaurant admin/owner of each store (for /admin/*). The customer must have ordered from BOTH stores so the cross-restaurant /account/orders list shows each row in its OWN store's timezone.

F. Long-name / RTL / theme edges: name one store with a very long name and an Arabic-locale admin login available if you want to spot RTL layout regressions in the date columns.

**Untested surface in this area:**
- Whether the SERVER actually runs in UTC in the environment under test — if the test server happens to be in the same tz as the store, a UTC regression will be invisible. Verify the deployment tz or seed with fixed Z-instants (setup C) so the assertion holds regardless of server tz.
- account/orders same-day branch: the diffDays===0 path renders TIME-only via toLocaleTimeString with tz — an order placed 'today' at 11:50pm local. Not yet proven that the TIME shown matches the store clock (vs the >7-day DATE branch).
- account/orders relative buckets 'Yesterday' / 'N days ago' are intentionally tz-INDEPENDENT (compare absolute instants). Not verified that this is acceptable to Luigi: a late-night order could read 'Yesterday' while the date-based surfaces read today's local date — confirm the two are allowed to disagree by design.
- account/page recent-orders date uses locale=undefined (browser/system locale) whereas account/orders uses 'en-US'. Not verified the two marketplace surfaces render the same month/day format for the same order.
- Connectivity daily-uptime LABEL uses new Date(`${dayKey}T12:00:00`).toLocaleDateString WITHOUT a timeZone option (noon-anchored, renders in server-local). Not verified on a server whose local tz is far negative (e.g. UTC-11) that noon still can't roll the label to the adjacent day.
- Connectivity BUCKET boundaries (parseLocalDateTimeInTz dayStart) vs the uptime math: not verified that a device online across local midnight is attributed to the correct local day's bar (the % could leak into the wrong day if bucketing regressed).
- Customer-detail coupon-expiry (couponExpires) and order-history row both use tzOpts — only reached when the customer HAS a code grant with endsAt in the past and >=1 order. Not verified those two specific cells render in-tz.
- reports/list/clients last-order shows DATE only (no time). A late order that is 'today' local but 'tomorrow' UTC must show today's local date — not verified the date column specifically (distinct from the range label which uses a different helper).
- No-timezone fallback: a restaurant with timezone unset (null) must not crash and should fall back to server/local formatting. Not verified any of the six surfaces with a null-timezone store.
- Export files (CSV) for List Orders / List Clients / Connectivity are separate code paths (…/export routes) and were NOT part of this diff — dates in the downloaded file may still be UTC. Flag to confirm export tz separately.

### 🔴 TZ-P0-01 — Admin Reports > List > Orders: late-evening order shows the store's LOCAL calendar date, not UTC (positive-offset Sydney)
- **Preconditions:** East store timezone = Australia/Sydney. Seeded order with createdAt = 2026-07-01T13:30:00Z (which is 2026-07-01 11:30pm Sydney local, and it IS still 2026-07-01 in Sydney but would be 2026-07-01 1:30pm UTC — pick instead 2026-07-01T13:30:00Z where Sydney = 11:30pm same day; to force a UTC-rollover trap use createdAt = 2026-06-30T15:30:00Z = 2026-07-01 1:30am Sydney but 2026-06-30 in UTC).
- **Steps:**
  1. Sign in as the East (Sydney) store admin.
  2. Go to Admin > Reports > List View > Orders (/admin/reports/list/orders).
  3. Set the date range to include the seeded order (use a custom range Jun 30 – Jul 2, 2026).
  4. Find the seeded order row and read the Date column.
- **Expected:** For createdAt = 2026-06-30T15:30:00Z the Date column shows 7/1/2026 (Jul 1) 1:30 AM — the SYDNEY local day — NOT 6/30/2026. If it shows Jun 30, the tz option is not being applied (regression).
- **Edges:** Positive-offset store: the correct local date is AHEAD of the UTC date. This is the case a plain UTC render gets wrong by showing yesterday.

### 🔴 TZ-P0-02 — Admin Reports > List > Orders: negative-offset (Pacific) late order rolls to PREVIOUS UTC day but shows correct local date
- **Preconditions:** West store timezone = America/Los_Angeles. Seeded order createdAt = 2026-07-01T06:30:00Z (= 2026-06-30 11:30pm Pacific).
- **Steps:**
  1. Sign in as the West (Los Angeles) store admin.
  2. Open /admin/reports/list/orders.
  3. Set a custom range Jun 29 – Jul 2, 2026.
  4. Locate the seeded order and read the Date column.
- **Expected:** Date column shows 6/30/2026, 11:30 PM (Pacific). It must NOT show 7/1/2026 (which is what raw UTC would print). The time must read 11:30 PM, not 6:30 AM.
- **Edges:** Late-night trap: 11pm local order that UTC calls the next day. Proves both the DATE and TIME are in-tz.

### 🔴 TZ-P0-03 — Marketplace /account/orders: each order row renders in its OWN restaurant's timezone (cross-restaurant)
- **Preconditions:** One customer account has one order at West (Pacific) store createdAt 2026-07-01T06:30:00Z (=Jun 30 11:30pm PT) and one at East (Sydney) store createdAt 2026-06-30T15:30:00Z (=Jul 1 1:30am AEST). Both must be older than 7 days OR ensure the date branch is exercised (see TZ-P1-08 for the same-day branch).
- **Steps:**
  1. Sign in as the customer at /account/login.
  2. Go to /account/orders (Your orders).
  3. Read the date shown at the end of each order row (after the status pill).
- **Expected:** The West-store row shows the PACIFIC local date (Jun 30) and the East-store row shows the SYDNEY local date (Jul 1) — each row uses ITS restaurant's timezone independently, not one global zone and not the server UTC clock.
- **Edges:** Cross-restaurant: two rows in two different zones on the same screen. A regression that hardcodes one zone (or UTC) makes at least one row wrong.

### 🔴 TZ-P0-04 — Admin Customer detail: First-seen / Last-order / Last-login all render in the restaurant timezone
- **Preconditions:** West (Pacific) store. A customer whose createdAt, lastOrderAt, and lastLoginAt are all late-evening-Pacific instants that cross UTC midnight (e.g. each = 2026-07-01T06:30:00Z → Jun 30 11:30pm PT).
- **Steps:**
  1. Sign in as the West store admin.
  2. Open Admin > Customers, click the seeded customer to open /admin/customers/<id>.
  3. Read the three stat lines under the profile header: First seen, Last order, Last login.
- **Expected:** All three dates read Jun 30, 2026, 11:30 PM (Pacific) — NOT Jul 1. formatDate now renders year/month/day + hour:minute in the store tz. A regression shows Jul 1 or a 6:30 AM time.
- **Edges:** Three call sites share tzOpts; verify all three, not just one. Last-login only appears for accounts with a password (signed-up, not guest).

### 🔴 TZ-P0-05 — Admin Customer detail: order-history row timestamps render in the restaurant timezone
- **Preconditions:** Same West store customer with a seeded order at createdAt = 2026-07-01T06:30:00Z (Jun 30 11:30pm PT).
- **Steps:**
  1. On /admin/customers/<id>, scroll to the Order history list.
  2. Read the date/time line under each order number.
- **Expected:** The order line shows Jun 30, 2026, 11:30 PM (Pacific), not Jul 1 / 6:30 AM. Uses toLocaleString with the store timeZone.
- **Edges:** Distinct call site from the header stats — the order-history cell has its own toLocaleString with tzOpts spread.

### 🔴 TZ-P0-06 — Admin Reports > List > Clients: Last-order DATE column uses the restaurant timezone
- **Preconditions:** West (Pacific) store. A customer whose most-recent order createdAt = 2026-07-01T06:30:00Z (Jun 30 11:30pm PT).
- **Steps:**
  1. Sign in as West store admin, open /admin/reports/list/clients.
  2. Set a date range covering the order (custom Jun 29 – Jul 2, 2026).
  3. Find the customer and read the Last order column.
- **Expected:** Last order shows Jun 30, 2026 (Pacific local day). It must NOT show Jul 1, 2026. This column is DATE-only (no time), so the whole point is the calendar day is the store's local day.
- **Edges:** Date-only surface — the exact case where a late-night order silently shifts a calendar day under UTC. Positive-offset variant: repeat on the Sydney store and confirm it shows the AHEAD local date.

### 🔴 TZ-P0-07 — Connectivity report: daily-uptime bars are bucketed + labelled by the restaurant's LOCAL calendar days
- **Preconditions:** East store timezone = Australia/Sydney with at least one ConnectivityEvent / device history in the selected range. Choose a multi-day range (e.g. last 7 days).
- **Steps:**
  1. Sign in as the East (Sydney) store admin.
  2. Open Admin > Reports > Online Ordering > Connectivity.
  3. Read the 'Daily uptime' bar labels (weekday + month + day) top to bottom.
  4. Cross-check the FIRST and LAST bar labels against the date range shown in the header.
- **Expected:** There is exactly one bar per Sydney-local calendar day in the range; the first/last labels match the range's local start/end day. No day is missing, duplicated, or off-by-one versus the header label. On a positive-offset store the local day boundary is many hours ahead of UTC, so a UTC-bucketing regression would produce a shifted or extra/short day.
- **Edges:** Positive-offset day-boundary case. Bar count must equal the number of local days in the range.

### 🔴 TZ-P0-08 — Connectivity report: device First-seen / Last-seen times render in the restaurant timezone
- **Preconditions:** East (Sydney) store with a registered kitchen device whose firstSeenAt / lastSeenAt are late-evening-Sydney instants that cross UTC midnight.
- **Steps:**
  1. On the Connectivity report, scroll to the Devices roster table.
  2. Read the First seen and Last seen columns for the device.
- **Expected:** Both timestamps show the SYDNEY local date+time (e.g. an instant of 2026-06-30T15:30:00Z shows Jul 1, 1:30 AM), not the UTC Jun 30 value.
- **Edges:** Device table cells are a separate toLocaleString call from the day buckets — verify independently.

### 🔴 TZ-P0-09 — DST boundary: offset is computed per-instant (spring-forward and fall-back land on the correct local time)
- **Preconditions:** West store = America/Los_Angeles. Seed two orders: createdAt = 2026-03-08T09:30:00Z (spring-forward day; PDT = UTC-7 → 2:30 AM PDT, just after the 2:00→3:00 skip) and createdAt = 2026-11-01T08:30:00Z (fall-back day; still PDT before 2:00 local → 1:30 AM).
- **Steps:**
  1. Sign in as West store admin, open /admin/reports/list/orders.
  2. Range Mar 7 – Mar 9, 2026; find the spring order, read its time.
  3. Change range to Oct 31 – Nov 2, 2026; find the fall order, read its time.
- **Expected:** Spring order (2026-03-08T09:30:00Z) shows 2:30 AM PDT (UTC-7), NOT 1:30 AM (which a fixed UTC-8 winter offset would give). Fall order (2026-11-01T08:30:00Z) shows 1:30 AM PDT. Times reflect the correct DST offset for each instant, proving Intl (not a hardcoded offset) is used.
- **Edges:** DST-boundary case required by the plan. The 2:00–3:00 spring gap and the 1:00–2:00 fall ambiguity are the classic off-by-one-hour traps.

### 🟡 TZ-P1-10 — Connectivity daily-uptime label uses noon-anchor and never rolls the labelled day
- **Preconditions:** Any store; a range spanning a month boundary (e.g. Jun 28 – Jul 3) so a label near midnight-UTC could roll if mis-anchored.
- **Steps:**
  1. Open the Connectivity report on a range that crosses a month boundary.
  2. Read every daily-uptime bar label and confirm each names a distinct, sequential calendar day.
- **Expected:** Labels read as consecutive local days (…Jun 30, Jul 1, Jul 2…) with no repeated or skipped day and no label off by one from its bucket. (Label is noon-anchored: `${dayKey}T12:00:00`, so it cannot roll a day even without a tz option.)
- **Edges:** Month-boundary + noon-anchor edge. If a label disagrees with the % it sits above (wrong day attributed), that's a bucket/label mismatch.

### 🟡 TZ-P1-11 — Marketplace /account/orders same-day branch shows the store-local TIME
- **Preconditions:** Place a REAL order today from the West (Pacific) store so it is <24h old (diffDays===0 path). Ideally near local late-evening.
- **Steps:**
  1. Sign in as the customer, open /account/orders.
  2. Find the just-placed order (should be at the top).
  3. Read the value at the end of the row.
- **Expected:** The row shows a TIME (e.g. 11:52 PM) in Pacific store time, not a date, and the time matches the store's wall clock — not the UTC time and not 'Yesterday'.
- **Edges:** Same-day branch uses toLocaleTimeString with tz. Late-night: an 11:52pm PT order must still read PM, not a shifted AM.

### 🟡 TZ-P1-12 — Marketplace /account dashboard 'Recent orders' date renders in the restaurant timezone
- **Preconditions:** Customer with an order at the East (Sydney) store createdAt = 2026-06-30T15:30:00Z (Jul 1 1:30am AEST).
- **Steps:**
  1. Sign in as the customer, land on /account.
  2. In the 'Recent orders' section, read the date on the Sydney-store row.
- **Expected:** Shows Jul 1 (Sydney local day), not Jun 30 (UTC). Format is month + day in the store timezone.
- **Edges:** account/page uses locale=undefined; confirm it still shows the correct local DAY even if the month/day formatting differs slightly from account/orders.

### 🟡 TZ-P1-13 — Customer-detail coupon expiry date renders in the restaurant timezone
- **Preconditions:** West (Pacific) store customer with an assigned promo/coupon grant whose promotion.endsAt is a late-evening-Pacific instant crossing UTC midnight and is in the PAST (so the 'expired' branch renders the couponExpires date).
- **Steps:**
  1. Open /admin/customers/<id> for that customer.
  2. In the 'coupons this customer has' card, find the expired grant and read the 'Expires …' date.
- **Expected:** The expiry date shows the Pacific local calendar day, not the UTC day.
- **Edges:** Only rendered for expired grants (endsAt in the past). Easy to miss because the card only appears when grants exist.

### ⚪ TZ-P2-14 — No-timezone store does not crash and falls back gracefully
- **Preconditions:** A store with timezone unset/null (or ask dev to null it on a scratch store) that has at least one order and one customer.
- **Steps:**
  1. As that store's admin, open /admin/reports/list/orders, /admin/reports/list/clients, /admin/customers/<id>, and the Connectivity report.
  2. As a customer of that store, open /account and /account/orders.
- **Expected:** Every page renders without error; dates fall back to server/local formatting (tz option simply omitted). No 500, no 'Invalid Date', no blank cells.
- **Edges:** Null-tz fallback path — the `tz ? {timeZone: tz} : {}` guard must degrade cleanly.

### ⚪ TZ-P2-15 — Long restaurant name + RTL admin locale do not break the date columns
- **Preconditions:** A store with a very long name; an Arabic (RTL) admin locale available.
- **Steps:**
  1. View /admin/reports/list/orders and the Connectivity device table with the long-name store.
  2. Switch the admin UI to Arabic and reload the same pages.
- **Expected:** Date columns stay readable and aligned; no overflow clipping the date, no layout break; in RTL the date cell mirrors correctly and the date text is still legible.
- **Edges:** Long-name + RTL layout edges from the standing checklist; dates are the newly-touched cells so verify they survive both.

---

## B. COUPON / MONEY

_Coupon empty-cart fix + single-best-deal ("bigger deal won") messaging on the customer order page_

**Setup:** WHAT CHANGED (final state @012a0144), so you know exactly what you're testing:
- src/app/api/public/coupon/route.ts line 63: the minimum-order gate is now `promo.minimumOrder > 0 && subtotal > 0 && subtotal < promo.minimumOrder`. Previously an EMPTY cart (subtotal 0) got a spurious "Minimum order X required" 400 and the coupon was never registered. Now an empty cart is accepted optimistically (returns source:"promotion") and the minimum is enforced later once items exist.
- The actual minimum-order enforcement + discount math live in src/lib/promo-engine.ts line 312 (`promo.minimumOrder > 0 && ctx.subtotal < promo.minimumOrder → not eligible`). This SAME engine (resolvePromotions) is called by BOTH the cart preview (src/app/api/public/apply-promos/route.ts) and the final order placement (src/app/api/orders/route.ts). That is why the preview total must equal the charged total — they run identical code.
- Cart UI strings (src/messages/en.json): dealsNotApplied="Available deals (tap to switch)", cantCombineWith="Can't combine with {winner}", useThisInstead="Use this instead", couponSwitchedSavesMore, couponBlocked, couponPromoApplied, promoEmailMismatch="This code is registered to a different email address."

PREREQUISITES / DATA TO CREATE IN ADMIN (do this once before the cases). All in the same restaurant (use the seeded demo store, e.g. slug demo-pizza-palace):
  PROMO A — "SAVE10" coupon-code promo (a HIDDEN promo carrying a couponCode):
    Admin > Promotions > New. Type: percentage or fixed discount (e.g. 10% off). Visibility: HIDDEN. Coupon code: SAVE10. Minimum order: $20.00. Active: yes. No start/end date (or a window covering today). Stacking: exclusive (default). Save.
  PROMO B — "HALFOFF" big AUTO deal that will out-save the coupon:
    Admin > Promotions > New. Type: percentage 50% off whole order. Visibility: VISIBLE (banner). Auto-apply: yes. Minimum order: $0. Stacking: exclusive. Active: yes. Save. (This is the "bigger deal" that should win over SAVE10.)
  PROMO C — an ASSIGNED/personal code for the wrong-email case:
    Admin > Customers > pick a customer whose email is ownerA@example.com > assign a promo/code, code = MYCODE10, 10% off, min order $0. This produces a Promotion + CustomerCoupon grant tied to ownerA@example.com.

  Note the restaurant currency symbol shown in admin (used to verify formatting). Have the customer order page open at /order/demo-pizza-palace in a normal browser tab. For the email-link path you can simulate the account button by loading /order/demo-pizza-palace?coupon=SAVE10 (the ?coupon= URL auto-apply runs applyCoupon on load with an empty cart — identical entry point to the account "Use this offer" button).

**Untested surface in this area:**
- Concurrency: two devices applying the same usage-limited coupon simultaneously — whether usageLimit is enforced atomically at order placement (not verified here; engine only reads usedCount, the atomic decrement/claim is in the order route and out of scope of this diff).
- The account 'Use this offer' BUTTON itself (not the ?coupon= URL) — assumed identical entry point but not exercised end-to-end from the account page in this plan.
- Reward Dollars interaction: how a registered coupon coexists with reward_credit payment at checkout (separate subsystem, not touched by this diff).
- Multi-menu / delivery-only promos where minimumOrder interacts with delivery fee — only whole-order subtotal min was tested.
- Exact toast wording/duration for couponBlocked (6000ms) vs couponSwitchedSavesMore in every one of the 38 locales — only en + one RTL locale spot-checked.

### 🔴 CPN-01 — P0 — "Use this offer" / email link on an EMPTY cart shows NO minimum-order error and registers the code
- **Preconditions:** PROMO A (SAVE10, min $20) exists and is active. Cart is empty (0 items).
- **Steps:**
  1. Open a fresh browser tab.
  2. Go to https://<yourhost>/order/demo-pizza-palace?coupon=SAVE10 (this is the exact same code path as the account 'Use this offer' button — applyCoupon runs on load with subtotal 0).
  3. Do NOT add any items yet. Watch the top-of-page toast/notification and the coupon field in the cart.
  4. Open the cart panel.
- **Expected:** NO red 'Minimum order of $20.00 required for this promo' error appears. No error toast. The code SAVE10 is now registered/pending (the coupon input shows SAVE10 pre-filled). The page does not crash or throw. Because there are no items yet, no discount line shows — but the code is remembered so that as soon as qualifying items are added the deal (or the 'bigger deal won' note) can appear. This is the core regression the change fixes: before, subtotal 0 returned a 400 and the coupon silently never registered.
- **Edges:** 0-item cart is the whole point here — confirm zero-item state produces no error. Also try the manual path: leave cart empty, type SAVE10 into the coupon box, press Apply → same result (no min-order error at subtotal 0).

### 🔴 CPN-02 — P0 — Coupon that alone would qualify is correctly SUPPRESSED by a bigger auto-deal, with the 'not applied / saved more' note + working 'Use this instead'
- **Preconditions:** PROMO A (SAVE10, 10% off, min $20) AND PROMO B (HALFOFF, 50% off auto, VISIBLE) both active. Start from CPN-01 (SAVE10 registered) or re-enter it.
- **Steps:**
  1. With SAVE10 registered, add items to the cart totalling clearly over $20 (e.g. two items = $30.00).
  2. Wait for the cart preview to re-evaluate (apply-promos runs automatically).
  3. Read the cart: the applied-deals area, the amber 'Available deals (tap to switch)' block, and the totals.
  4. Click the 'Use this instead' button next to SAVE10 in the amber block.
  5. Wait for re-evaluation and re-read the totals.
- **Expected:** Before clicking: the 50% HALFOFF is applied as the winner (largest discount, ~$15.00 off $30). SAVE10 appears under the amber 'Available deals (tap to switch)' header with the sub-text 'Can\'t combine with <HALFOFF name>' and a 'Use this instead' button. Only ONE discount is applied (single best deal — they are both exclusive, so they do not stack). The discount amount matches 50% not 10%.
After clicking 'Use this instead': the winner swaps — HALFOFF is suppressed, SAVE10's 10% now applies (discount ~$3.00), and HALFOFF moves into the amber 'Available deals' list so it can be tapped back. Note: in this case swapping to the SMALLER deal is a deliberate customer choice and is allowed.
- **Edges:** LONG NAMES: give PROMO B a long name (e.g. 'Grand Half-Price Weekend Blowout Special') and confirm the amber note truncates cleanly (the name span has truncate) without breaking layout. THEME COLORS: on /order/ pages the amber note is a fixed status color, but confirm surrounding buttons/banners use the restaurant theme.primaryColor, not hardcoded orange.

### 🔴 CPN-03 — P0 — When the entered coupon SAVES MORE than the current auto-deal, it auto-swaps in with the 'saves more' toast
- **Preconditions:** Create a variant: PROMO B set to a SMALL auto-deal (e.g. 5% off) and PROMO A SAVE10 = 20% off, min $20. Both exclusive/active.
- **Steps:**
  1. Start with an empty cart, add items over $20 (e.g. $30).
  2. Confirm the 5% auto-deal is applied (~$1.50 off).
  3. Type SAVE10 in the coupon box and press Apply.
  4. Watch the toast and the totals.
- **Expected:** Because the just-entered SAVE10 (20% = $6.00) saves MORE than the applied 5% auto-deal ($1.50), the engine auto-swaps: SAVE10 is applied, the 5% deal is removed, and a toast 'SAVE10 applied — it saves you more, so we removed <deal>. Tap its banner to switch back anytime.' (couponSwitchedSavesMore) appears. Total reflects the 20% discount, not both.
- **Edges:** This exercises the pendingCoupon effect's auto-swap branch (blocked.discount > winnerDiscount + 0.005). Confirm no double-discount.

### 🔴 CPN-04 — P0 — Coupon ACTUALLY applies and discounts the total once competing deals are OFF
- **Preconditions:** PROMO A (SAVE10, 10% off, min $20) active. Deactivate PROMO B (HALFOFF) in admin (toggle inactive) so there is no competing exclusive deal.
- **Steps:**
  1. Refresh the order page. Empty cart. Register SAVE10 (type + Apply, or ?coupon=SAVE10).
  2. Add items totalling over $20 (e.g. $30.00).
  3. Read the totals block.
- **Expected:** SAVE10 applies cleanly: a discount line shows -$3.00 (10% of $30), Subtotal $30.00, discount -$3.00, and the order total is reduced by exactly $3.00. A 'Promo "SAVE10" applied!' toast (couponPromoApplied) appears. No amber 'Available deals' block (nothing competing). The 'codeApplied' green row shows the code.
- **Edges:** Verify the discount uses the restaurant currency via formatCurrency (correct symbol + decimals), not a raw number.

### 🔴 CPN-05 — P0 — Minimum order is STILL enforced: below-minimum cart does not apply the coupon, at preview AND at checkout
- **Preconditions:** PROMO A (SAVE10, 10% off, min $20) active; competing deals OFF (from CPN-04).
- **Steps:**
  1. Empty cart, register SAVE10.
  2. Add items totalling UNDER $20 (e.g. one item = $12.00). Read the totals.
  3. Note whether any SAVE10 discount line appears.
  4. Proceed to checkout / Place Order and inspect the order summary and the final charged amount.
  5. Then add another item to cross $20 (e.g. total $24) and confirm the discount appears; place the order and re-check the charge.
- **Expected:** At $12 (below min): NO SAVE10 discount line in the preview — the promo engine (promo-engine.ts:312) treats it as ineligible, so it simply is not applied. No spurious error either. At checkout the final total = $12 + fees with NO promo discount (min-order re-enforced by the same engine in api/orders — preview and charge agree). After crossing $20: the 10% discount now appears in preview AND the placed order is charged the discounted total. This is the guard that the empty-cart fix did NOT weaken min-order enforcement — it only deferred it past subtotal 0.
- **Edges:** Critical money/correctness case. Confirm there is NO window where an empty-cart optimistic accept lets a below-minimum order actually apply the discount at checkout.

### 🔴 CPN-06 — P0 — Preview total EQUALS charged total (no drift between cart and payment)
- **Preconditions:** Any applied-coupon scenario from CPN-04 (SAVE10 applied, $30 cart, -$3.00).
- **Steps:**
  1. In the cart, record the exact final total shown (subtotal - discounts + delivery/fees + tax).
  2. Proceed to Place Order.
  3. On the payment/confirmation screen record the amount charged.
  4. Compare the two figures to the cent.
- **Expected:** The amount charged is identical to the cart preview total, to the cent. Both are computed by resolvePromotions over the same cart, so any mismatch is a real bug. Verify for: coupon-applied, coupon-suppressed-by-bigger-deal (charge reflects the winner), and below-minimum (no discount) states.
- **Edges:** Run this once per major state (applied / suppressed / below-min). A discrepancy here is a P0 payments defect.

### 🔴 CPN-07 — P0 — Assigned code entered with the WRONG email shows 'registered to a different email' and does NOT discount
- **Preconditions:** PROMO C (MYCODE10) assigned to ownerA@example.com. Use a different customer email at checkout.
- **Steps:**
  1. Open the order page, add items over any minimum.
  2. In the checkout/customer info, enter an email that is NOT ownerA@example.com (e.g. someoneelse@example.com).
  3. Type MYCODE10 in the coupon box and Apply.
  4. Read the coupon area and the totals.
- **Expected:** The amber note 'This code is registered to a different email address.' (promoEmailMismatch) appears under the coupon input (shown live in the cart, driven by apply-promos promoCodeEmailMismatch). NO discount is applied. Then change the email to ownerA@example.com and re-evaluate → the note disappears and MYCODE10's discount applies. Confirms ownership is enforced server-side, not just hidden in UI.
- **Edges:** Confirm the mismatch note only shows when a code is actually entered (couponCode.trim() truthy) and not applied — an empty box must not show it.

### 🟡 CPN-08 — P1 — Late-night / DST: order placed near midnight and across a DST boundary keeps the promo date-window correct
- **Preconditions:** PROMO A given a start/end window (e.g. valid only today in the restaurant's timezone). Restaurant in a DST-observing tz.
- **Steps:**
  1. Set the device/browser clock (or test in a tz) to just before local midnight on the last valid day.
  2. Register SAVE10 and add qualifying items.
  3. Confirm it applies.
  4. Advance to just after midnight (now outside the window) and re-evaluate.
- **Expected:** Before local midnight: coupon applies. After: coupon no longer applies (out of window) with no crash. Day boundary is judged consistently (the coupon route compares absolute instants via new Date(); confirm the promo does not flicker on/off due to a UTC-vs-local off-by-one).
- **Edges:** DST + late-night edge. Not part of this diff's core, but the empty-cart entry via ?coupon= could arrive at any hour — confirm no timezone rollover mis-classifies an active promo as expired.

### 🟡 CPN-09 — P1 — RTL locale renders the coupon note, amber deals block, and 'Use this instead' correctly
- **Preconditions:** Switch UI locale to an RTL language (e.g. Arabic 'ar' or Hebrew 'he'). PROMO A + PROMO B active.
- **Steps:**
  1. Set the order page locale to Arabic.
  2. Reproduce CPN-02 (bigger deal suppresses coupon).
  3. Inspect the amber 'Available deals' block, the 'Can\'t combine with {winner}' line, the 'Use this instead' button, and the mismatch note from CPN-07.
- **Expected:** All strings are translated (not English fallback), the layout mirrors correctly for RTL (button on the correct side, icons/text not overlapping), and {winner}/{name} placeholders interpolate. No clipped or reversed punctuation. The 'Use this instead' button still swaps the deal correctly.
- **Edges:** RTL + placeholder interpolation. Cross-check en.json keys have parity in the RTL locale (dealsNotApplied, cantCombineWith, useThisInstead, promoEmailMismatch, couponSwitchedSavesMore).

### 🟡 CPN-10 — P1 — Usage-limit and inactive/expired code still reject at code entry (regression guard)
- **Preconditions:** Create PROMO D with usageLimit reached (usedCount >= usageLimit), and note SAVE10 can be toggled inactive.
- **Steps:**
  1. Enter the usage-limit-exhausted code and Apply.
  2. Enter a non-existent code (e.g. NOPE123) and Apply.
  3. Toggle SAVE10 inactive in admin, refresh, enter SAVE10 and Apply.
- **Expected:** Usage-limit-reached → error 'This promo has reached its usage limit'. Unknown code → 'Invalid or expired coupon'. Inactive/expired code → 'Invalid or expired coupon'. These 400s are UNCHANGED by the empty-cart fix (only the min-order branch was gated on subtotal>0), so confirm they still fire even on an empty cart.
- **Edges:** Confirms the fix narrowly touched only the min-order branch and didn't loosen other validations.

### ⚪ CPN-11 — P2 — Rate limiting on coupon attempts still active
- **Preconditions:** None.
- **Steps:**
  1. Rapidly submit 11+ different coupon codes within one minute from the same browser/IP.
  2. Observe the response after the 10th attempt.
- **Expected:** After 10 attempts/minute the endpoint returns 'Too many attempts. Please wait before trying again.' (429). Unchanged by this diff; regression guard only.
- **Edges:** Brute-force protection — low priority but verify it wasn't disturbed.

---

## C. BANNERS / DISH-COUNT / COLLAPSE

_Customer order page — category banners, dish-count pill, banner/pill hover, and start-collapsed accordion (src/app/order/[slug]/OrderingPageClient.tsx, CategoryBanner + collapse-seed effect; final state at 012a0144)_

**Setup:** GLOBAL SETUP — do this once before the cases below.

WHERE THE CONTROLS LIVE (all in the admin, one screen):
1. Log into the admin and open Website > Theme (URL path /admin/website/theme).
2. On that screen you have four controls you will change repeatedly:
   - "Menu layout" — three buttons: Carousel / Grid / List. The selected one is highlighted green. (This is theme.menuLayout.)
   - "Show category images" — a green/grey toggle. ON (green) = each category shows a big photo BANNER; OFF (grey) = a plain text heading with NO banner. (theme.showCategoryImages.) CRITICAL: the dish-count pill and the banner hover-darken ONLY exist when this is ON. When it is OFF there is deliberately no banner and no pill — that is correct, not a bug.
   - "Collapsible categories" — a green/grey toggle. ON = the accordion (categories start closed, Expand all / Collapse all controls appear). (theme.mobileCollapsibleCategories.) Despite the internal name it now applies to BOTH phone and computer.
   - "Primary colour" — the top colour picker. This is the brand colour used as the banner background when a category has NO photo.
3. After changing ANY of these, click Save, then open the live customer page in a separate tab: /order/<your-restaurant-slug> . Hard-refresh (Ctrl+F5) after each save so you see the new setting.

MENU DATA YOU NEED (set up once in Menu > Categories/Items):
- At least 4 categories so the top pill row and the accordion are meaningful.
- Category A: HAS a category image uploaded, a SHORT name (e.g. "Pizza"), and 3+ dishes.
- Category B: NO category image (leave the image blank so it uses the themed colour fallback), a short name, 5+ dishes.
- Category C: a VERY LONG name (paste exactly: "Wood-Fired Neapolitan Pizzas, Calzones & Family-Size Sharing Platters"), with a photo, 12+ dishes (so the number is 2 digits).
- Category D: a category with ZERO dishes currently available. Easiest: create a category "Seasonal Specials" and either add no items or mark all its items unavailable. NOTE: a category with 0 visible items is filtered out of the customer page entirely, so to actually SEE a 0-count you must instead check the pill's behaviour by temporarily reducing a category to exactly the boundary — see TC-DISH-03 for the exact method.

HOW TO SWITCH DEVICE VIEW without a phone: in the customer tab open the browser dev tools (F12), click the little phone/tablet "device toolbar" icon, pick "iPhone 12 Pro" for mobile and "Responsive / 1280px" for desktop. Reload after switching. (If you have a real phone, just open the /order URL on it.)

TWO BRAND COLOURS TO TEST: a DARK primary (paste #7C2D12, a deep brown) and a PALE/LIGHT primary (paste #FFF3C4, a pale cream). The pale one triggers the "dark text on light banner" code path.

**Untested surface in this area:**
- Whether the collapse-seed re-fires correctly when the viewport crosses the mobile/desktop breakpoint mid-session (isMobile is in the effect deps but collapseSeededRef prevents re-seeding — behaviour on a live resize after the customer manually opened some categories is not covered by a device-emulation reload).
- Search interaction: when a menu search is active the accordion is suspended (collapsibleActive=false). Not covered here — searching should temporarily expand all matches, then restore collapsed state when cleared.
- Carousel banner scroll ARROWS (left/right) on a banner that is a carousel but NOT collapsible — the onScroll path vs onToggleCollapse path share the top-right corner; interaction between arrows and the hover tint on a non-collapsible carousel banner is not exercised.
- Exact colour-contrast/accessibility (WCAG) numbers for the pill text on borderline brand colours near the isLightColor threshold — only eyeballed, not measured.
- The promo-strip collapse (promosCollapsed) shares the chevron pattern but is out of scope of this banner change and untested here.
- Keyboard/screen-reader: banner has role=button + aria-expanded but keyboard Enter/Space activation and SR announcement of the count pill (aria-hidden icon) are not verified.

### 🔴 TC-COLLAPSE-01 — P0 — Every category starts CLOSED on DESKTOP when accordion is on (was expanded before)
- **Preconditions:** Website>Theme: Menu layout = Carousel, Show category images = ON, Collapsible categories = ON, Primary = #7C2D12. Save.
- **Steps:**
  1. Open /order/<slug> in a fresh tab on a DESKTOP-width view (Responsive 1280px).
  2. Hard-refresh (Ctrl+F5) so this is a first load.
  3. Look at the menu area below the category pill row.
- **Expected:** Every category shows only its banner/header with a downward chevron — NO dishes are visible under any category. The whole menu is a compact stack of closed banners. (This is the behavioural change: before 012a0144 desktop opened fully expanded; now desktop starts fully collapsed like mobile.)
- **Edges:** This is the core regression to confirm — desktop previously started EXPANDED. Also confirm 'Expand all | Collapse all' controls appear just above the menu.

### 🔴 TC-COLLAPSE-02 — P0 — Every category starts CLOSED on MOBILE (unchanged behaviour, must still hold)
- **Preconditions:** Same theme as TC-COLLAPSE-01.
- **Steps:**
  1. Switch the customer tab to mobile view (iPhone 12 Pro) or open the URL on a real phone.
  2. Hard-refresh.
  3. Observe the menu.
- **Expected:** All categories start closed (banners only, no dishes shown), identical to desktop. Nothing regressed on mobile.
- **Edges:** Mobile was already collapsed-by-default; confirm the seed change didn't accidentally break mobile.

### 🔴 TC-COLLAPSE-03 — P0 — Tapping a banner EXPANDS that category and scrolls to it
- **Preconditions:** Same theme; menu fully collapsed from TC-COLLAPSE-01.
- **Steps:**
  1. On the collapsed menu, click (desktop) or tap (mobile) anywhere on Category A's banner (not on the chevron specifically — anywhere on the band).
  2. Observe.
- **Expected:** Category A expands to show its dishes, its chevron flips to point UP, and the page smoothly scrolls so the top of that category is in view. Other categories stay closed.
- **Edges:** The whole banner band is the button (role=button). A second click/tap on it should collapse it again (chevron flips back down).

### 🔴 TC-COLLAPSE-04 — P0 — Tapping a TOP PILL expands the matching category and scrolls to it
- **Preconditions:** Same theme; menu fully collapsed.
- **Steps:**
  1. In the sticky category pill row at the top, click/tap the pill for Category B.
  2. Observe the menu below.
- **Expected:** Category B becomes the active pill (fills with the brand colour), Category B expands to reveal its dishes, and the page scrolls to the top of Category B. If B was already open it simply scrolls there.
- **Edges:** Confirms scrollToCategory un-collapses the target (scrollToCategory removes the id from collapsedCats when collapsibleActive). A pill tap must never scroll you to a still-collapsed header.

### 🟡 TC-COLLAPSE-05 — P1 — Expand all / Collapse all controls work
- **Preconditions:** Same theme; menu fully collapsed.
- **Steps:**
  1. Click 'Expand all' (just above the menu).
  2. Then click 'Collapse all'.
- **Expected:** 'Expand all' opens every category at once (all dishes visible, all chevrons up). 'Collapse all' closes every category (banners only, chevrons down).
- **Edges:** These labels are translated; in an RTL locale they should still function and read right-to-left.

### 🟡 TC-COLLAPSE-06 — P1 — Accordion OFF: categories render fully expanded (no regression when feature disabled)
- **Preconditions:** Website>Theme: Collapsible categories = OFF. Keep Show category images = ON, Carousel. Save.
- **Steps:**
  1. Reload /order/<slug> on desktop and on mobile.
- **Expected:** No chevrons on banners, no Expand/Collapse controls, all categories show their dishes normally. The banner is NOT clickable (no cursor-pointer, no hover-darken).
- **Edges:** The hover-darken tint is gated on collapsible=true; with the accordion off there must be NO hover darkening on the banner.

### 🔴 TC-DISH-01 — P0 — Dish-count pill shows the CORRECT number on a banner
- **Preconditions:** Website>Theme: Carousel, Show category images = ON, Primary = #7C2D12. Accordion can be ON or OFF. Save. Note the exact number of visible dishes in Category A (count them in the admin menu).
- **Steps:**
  1. Open /order/<slug>. If collapsed, that's fine — the pill is on the banner itself.
  2. Read the little pill in the LOWER-LEFT of Category A's banner: a fork/knife icon followed by a number.
- **Expected:** The number equals the count of currently-visible dishes in Category A (e.g. if A has 3 available dishes, the pill reads a fork icon + '3'). Change one dish to unavailable, reload, and the number drops by one.
- **Edges:** Count is cat.menuItems.length AFTER the visible/available filter — must match exactly what the customer can see, not the raw admin count.

### 🔴 TC-DISH-02 — P0 — Two-digit count doesn't overflow or collide with the category name
- **Preconditions:** Category C (long name, photo, 12+ dishes). Carousel, Show category images = ON. Save.
- **Steps:**
  1. Open /order/<slug> and locate Category C's banner on both desktop and mobile (narrowest view).
  2. Look at the long name and the count pill together.
- **Expected:** The long name is truncated with an ellipsis on one line; directly under it sits the count pill (fork icon + a 2-digit number like '12'). The pill sits neatly under the name, does not wrap awkwardly, does not overflow the banner edges, and does not overlap the chevron/scroll buttons in the top-right.
- **Edges:** Long-name edge: name is truncate + the text block is right-padded (right-14) to clear the top-right controls; confirm no collision. Two-digit number edge (pill widens).

### 🟡 TC-DISH-03 — P1 — Pill is HIDDEN when a category has 0 dishes
- **Preconditions:** Carousel, Show category images = ON.
- **Steps:**
  1. In the admin, take one category with a photo and set ALL its items to unavailable (or delete them) so it has 0 visible dishes — but to keep it on the page temporarily, leave exactly one placeholder item visible first and confirm the pill reads '1'.
  2. Now hide that last item so the category has 0 visible dishes and reload the customer page.
- **Expected:** With 1 item the pill reads '1'. With 0 visible items the category is removed from the customer page entirely (it no longer appears at all) — so there is never a banner showing a bare fork icon with no number. The pill only renders when the count is greater than 0.
- **Edges:** 0-item edge: the pill is wrapped in `menuItems.length > 0` AND a 0-item category is filtered off the page — verify BOTH: no zero pill, and no empty-category banner.

### 🟡 TC-DISH-04 — P1 — Dish-count pill is readable on a PALE brand colour (dark-text path) and on a photo
- **Preconditions:** Category A (photo). Category B (NO photo). Set Primary = #FFF3C4 (pale cream). Carousel, Show category images = ON. Save.
- **Steps:**
  1. Open /order/<slug>.
  2. Compare Category A's pill (over its photo) and Category B's pill (over the pale cream fallback band).
- **Expected:** On Category A (photo): white-ish pill with white text/icon, legible over the dark photo scrim. On Category B (pale cream, no photo): the name and the pill switch to DARK text, and the pill uses a subtle dark translucent chip — the number and fork icon are clearly readable against the pale band (no white-on-cream invisibility).
- **Edges:** Theme-colour edge: pale colour flips overlayText to dark (#1f2937) and the pill background to rgba(0,0,0,0.10). Verify contrast is acceptable.

### ⚪ TC-DISH-05 — P2 — Dish-count pill appears the same in ALL layouts that show banners; absent when images are OFF
- **Preconditions:** Show category images = ON.
- **Steps:**
  1. Set Menu layout = Carousel, save, reload — note the pill on Category A's banner.
  2. Set Menu layout = Grid, save, reload — check Category A.
  3. Set Menu layout = List, save, reload — check Category A.
  4. Now set Show category images = OFF, save, reload — check Category A.
- **Expected:** Carousel, Grid, and List all use the same CategoryBanner and therefore all show the fork+number pill identically. With Show category images OFF there is NO banner and NO pill — just a plain text heading with the dish name; that is correct.
- **Edges:** The pill lives inside CategoryBanner, which only renders in the showCategoryImages path across all three layouts. Images-OFF path has no banner/pill by design.

### 🟡 TC-HOVER-01 — P1 — DESKTOP hover darkens a clickable (collapsible) banner
- **Preconditions:** Carousel, Show category images = ON, Collapsible categories = ON, Primary = #7C2D12. Save. Desktop view with a real mouse (device-emulation may not fire hover — use a normal desktop browser window at full size).
- **Steps:**
  1. Open /order/<slug> on desktop.
  2. Move the mouse pointer over Category A's banner and hold it there (don't click).
- **Expected:** While hovered, the whole banner band darkens slightly (a ~20% black tint fades in over ~0.2s). Moving the pointer off restores it. The cursor is a pointing hand over the banner.
- **Edges:** Pointer-only: this is expected to do NOTHING on a touchscreen (see TC-HOVER-03). Tint only exists when collapsible (accordion ON).

### 🟡 TC-HOVER-02 — P1 — DESKTOP hover darkens the TOP category pills (matches banner hover)
- **Preconditions:** Same as TC-HOVER-01 (accordion state doesn't matter here — pills always hover).
- **Steps:**
  1. On desktop, hover the mouse over one of the top category pills (both the active brand-coloured pill and an inactive pale one).
- **Expected:** Each pill darkens ~10% while hovered (brightness reduces) and the cursor is a pointing hand. This applies whether the pill is the active brand-colour one or an inactive one. Releasing hover restores the colour.
- **Edges:** hover:brightness-90 works over inline theme colours. Unlike the banner tint, pill hover is NOT gated on the accordion — verify it works with Collapsible categories OFF too.

### ⚪ TC-HOVER-03 — P2 — Hover tint does NOT get stuck on touch devices
- **Preconditions:** Same theme. Real phone/tablet if possible.
- **Steps:**
  1. On a touchscreen, tap a banner to expand a category, then tap elsewhere.
  2. Observe the banner after the tap.
- **Expected:** The banner expands/collapses on tap but does NOT stay darkened afterward (no sticky hover tint left behind). Tapping is clean.
- **Edges:** group-hover is pointer-hover; on touch it should be a no-op. Watch specifically for a stuck dark overlay after tap.

### 🟡 TC-SIZE-01 — P1 — Banner height/size looks right across the layout matrix (size was reverted to original)
- **Preconditions:** Show category images = ON, Primary = #7C2D12.
- **Steps:**
  1. For each Menu layout (Carousel, Grid, List): save, reload /order/<slug>, and view Category A's banner on both a wide desktop window AND a narrow phone view.
  2. Check the banner proportions.
- **Expected:** The banner is a comfortable band (roughly 130px tall on narrow screens up to ~156px on wide screens) with the category name, the gold/white accent bar, and the count pill sitting in the lower-left, and the chevron/scroll control in the upper-right. It should NOT look squashed/tiny or overly tall. The name is one truncated line.
- **Edges:** Size was shrunk then REVERTED (net zero) — confirm it matches the ORIGINAL comfortable size, not the intermediate smaller one. Check both clamp ends: very narrow (~130px floor) and very wide (~156px cap).

### ⚪ TC-SIZE-02 — P2 — Themed fallback banner (no photo) renders correctly with brand colour
- **Preconditions:** Category B (NO photo). Test once with Primary = #7C2D12 (dark) and once with #FFF3C4 (pale). Show category images = ON.
- **Steps:**
  1. Reload /order/<slug> with the dark primary; view Category B's banner.
  2. Change Primary to the pale cream, save, reload; view Category B again.
- **Expected:** Dark primary: banner is the deep brown with a soft diagonal dark wash, WHITE name + white accent bar + white-chip pill. Pale primary: banner is pale cream with DARK name, dark accent bar, and dark-chip pill — all legible. No black photo-scrim appears (there's no photo).
- **Edges:** isLightColor(primary) drives the light/dark text switch. Pale colour must flip everything to dark text; verify the accent bar and pill both flip, not just the name.

### 🟡 TC-RTL-01 — P1 — Arabic (RTL) locale: banner, pill, name, and controls lay out correctly
- **Preconditions:** Carousel, Show category images = ON, Collapsible = ON. Category C (long name) present.
- **Steps:**
  1. Open the customer page with Arabic selected — append ?locale=ar or use the on-page language switcher: /order/<slug>?locale=ar .
  2. View the banners, the count pill, the long-name category, and the top pill row on both desktop and mobile.
- **Expected:** Page text reads right-to-left. The category name is still legible and truncates correctly; the fork+number pill still renders (the number stays a Western/Arabic-Indic numeral per the locale) and does not collide with the name. Top pills, Expand all / Collapse all, and the chevrons remain usable. Tapping a banner/pill still expands + scrolls.
- **Edges:** RTL edge: verify the count pill and the top-right chevron/scroll buttons don't overlap the (now right-aligned) text; the pill is language-neutral (icon+number) so it needs no translation — confirm that holds in RTL.

### ⚪ TC-RTL-02 — P2 — Long name + RTL + 2-digit count together (worst-case wrap)
- **Preconditions:** Arabic locale, Category C (long name, 12+ dishes), Show category images = ON.
- **Steps:**
  1. In Arabic, on the narrowest mobile view, open Category C's banner and inspect the name + pill.
- **Expected:** Long name truncates on one line, the 2-digit count pill sits under it without overflowing the banner or colliding with the top-corner control, and nothing is clipped off-screen.
- **Edges:** Stacks three edges (long name + RTL + 2-digit). This is the most likely place for a layout collision.

---

## D. HOT-PATH REGRESSION

_Hot-path regression: customer order + coupon + checkout + kitchen + timezone-aware admin/reports/account dates + order-page category banner UI_

**Setup:** Use the seeded demo store (slug `demo-pizza-palace`) or any live store that has: (a) at least 2 menu categories each with several dishes, plus one empty category if possible; (b) at least one VISIBLE promo AND one code-only (hidden) promo with a MINIMUM ORDER set (e.g. min $25); (c) the accordion feature ON for at least one test store (theme.mobileCollapsibleCategories = true) and OFF for another; (d) a restaurant timezone that DIFFERS from the server/browser clock (e.g. set the store tz to America/Los_Angeles or Australia/Sydney so UTC-vs-local drift is visible). Have a customer account that already has at least one past order (for the account pages) and one recent late-night order (placed between ~20:00 and 02:00 local) to exercise day-boundary drift. Log in separately as: the restaurant admin (owner) and as a customer. Have the Kitchen Order App / kitchen display open and logged in for the SAME store. Note the store's currency and its 12h/24h hours format. For the coupon-empty-cart case you need the promo's shareable code and/or the account "Use this offer" button reachable from /account.

**Untested surface in this area:**
- Actual money math when a below-minimum coupon reaches the order route (orders/route.ts:1042 gates credit on serverSubtotal >= coupon.minimumOrder) — verify no discount is ever written to the order total below minimum on-device
- Delivery vs pickup min-order interaction: orders/route.ts:995 skips the restaurant minimum for delivery — confirm coupon min-order still behaves for delivery carts
- DST transition day: a store observing DST with an order/uptime event on the spring-forward or fall-back day (parseLocalDateTimeInTz + noon-anchored label) — not yet verified across a real DST boundary
- Stores with timezone unset/null: formatDate/toLocaleString fall back to {} (server/browser clock) — confirm those stores still render a sensible date and don't crash
- Locale-specific date formatting: the account page hardcodes 'en-US' for the same-day time and >7-day date even though the store may be in another locale — verify the restaurant's 12h/24h hours-format expectation isn't violated on customer-facing dates
- Accordion breakpoint change while browsing: resizing across the mobile/desktop breakpoint mid-session (collapseSeededRef guards re-seeding) — verify categories don't unexpectedly re-collapse or lose the customer's expand choices
- Connectivity report overall uptime % after switching from UTC to local-day bucketing — confirm the headline uptime number is still sensible for a multi-timezone store
- Kitchen thermal receipt date/time rendering — not touched by these diffs but sits adjacent to the tz work; confirm printed timestamps still correct
- Very large category (hundreds of dishes) — pill number display and truncation not verified at scale

### 🔴 P0-ORDER-E2E — Complete a real customer order end-to-end and confirm kitchen receipt
- **Preconditions:** Store open/accepting orders; kitchen app logged in to same store; customer on /order/<slug>.
- **Steps:**
  1. Open /order/<slug> as a customer.
  2. Add 2-3 dishes from at least two different categories to the cart.
  3. Open the cart and verify item names, quantities, and line prices are correct and totals add up in the store currency.
  4. Proceed to checkout, fill required contact/fulfilment fields, choose a payment method, and PLACE the order.
  5. Watch the kitchen display / Kitchen Order App for the new order to arrive and ring.
- **Expected:** Order places successfully with no error. The order confirmation screen shows the correct order number, items, and total. Within a few seconds the kitchen display receives the exact same order (items, quantities, total match) and rings/prints as normal. No regression from the OrderingPageClient banner/accordion edits.
- **Edges:** Run once on a store with the accordion ON (categories start collapsed) and once OFF, to prove adding-to-cart still works when categories are collapsed by default.

### 🔴 P0-COUPON-VALID-CART — Apply a min-order coupon on a cart that MEETS the minimum still works
- **Preconditions:** Promo exists with minimum order (e.g. $25).
- **Steps:**
  1. Add dishes totalling ABOVE the promo minimum (e.g. $30 when min is $25).
  2. Enter/apply the promo code in the cart.
  3. Verify the discount applies.
  4. Proceed to checkout and place the order.
- **Expected:** Coupon is accepted, discount is shown and deducted correctly, and the placed order reflects the discount. This proves the coupon route change (adding `subtotal > 0`) did NOT weaken enforcement for a real cart.
- **Edges:** Confirm the discounted total matches what the confirmation and kitchen ticket show.

### 🔴 P0-COUPON-BELOW-MIN — Coupon on a NON-empty cart BELOW the minimum is still rejected/not credited
- **Preconditions:** Promo with minimum order $25.
- **Steps:**
  1. Add dishes totalling BELOW the promo minimum (e.g. $10 when min is $25).
  2. Apply the promo code.
  3. Observe the cart messaging.
  4. Attempt to checkout and place the order.
- **Expected:** The minimum-order rule is still enforced for a real (non-empty) cart: the discount does NOT get credited to the final total. The customer is not silently given the discount below minimum. (Enforcement now lives in apply-promos/promo-engine and the order route rather than the coupon GET, so the money outcome must be unchanged.)
- **Edges:** This is the core money-correctness regression check for the coupon diff. The old code rejected at the coupon lookup; the new code accepts the code optimistically but MUST NOT actually discount below minimum.

### 🔴 P0-COUPON-EMPTY-CART — Arriving at the store via 'Use this offer' / email link with an EMPTY cart no longer errors
- **Preconditions:** A promo with a minimum order, reachable via the account 'Use this offer' button or a promo email link.
- **Steps:**
  1. Log in as a customer, go to /account (or open the promo email).
  2. Click 'Use this offer' (or the email link) which lands on the order page BEFORE any items are added.
  3. Observe the page immediately (empty cart).
- **Expected:** No spurious 'Minimum order of $X required' error appears on arrival. The coupon is registered/remembered on the empty cart. As items are added and the cart eventually meets the minimum, the discount applies; if it never meets minimum, the 'not applied — a bigger deal won' style cart message can now show. This is the exact bug the diff fixes.
- **Edges:** 0-item cart is the explicit edge. Verify with a promo that HAS a minimum (subtotal 0 path).

### 🔴 P0-KITCHEN-POLL — Kitchen display poll unaffected — new orders keep arriving/ringing
- **Preconditions:** Kitchen app logged in.
- **Steps:**
  1. With the kitchen display open, place a fresh order from the customer side (see P0-ORDER-E2E).
  2. Confirm it appears, rings, and can be accepted.
  3. Leave the display running and place a second order ~1 minute later.
- **Expected:** Both orders arrive within the normal poll interval, ring, and print. No change to kitchen behavior — none of these diffs touch the kitchen poll or order-notification path, so this is a guard against unexpected coupling.
- **Edges:** Confirm the accept ring + countdown still behave per the verified kitchen build.

### 🔴 P0-REPORTS-EOD-NUMBERS — Reports/EOD dashboard totals are numerically unchanged
- **Preconditions:** Admin logged in; store with recent orders.
- **Steps:**
  1. Before/independently, note the current day's order count and sales total from the dashboard / EOD.
  2. Open /admin/reports and the Orders list and Clients list.
  3. Compare the aggregate order counts and sales totals against the dashboard/EOD figures for the same range.
- **Expected:** All money and count aggregates are IDENTICAL to before. The timezone diffs only changed how individual DATES are DISPLAYED (formatting), not how orders are counted or summed. No total shifts.
- **Edges:** The connectivity report changed day BUCKETING (UTC->local days). For a store in a non-UTC tz, a late-night event may now fall on a different calendar day than before — that is the intended fix, not a regression. Only uptime %-per-day bucketing should shift; order SALES totals must not.

### 🔴 P0-LATE-NIGHT-DATE — Late-night order shows correct local day across account + reports
- **Preconditions:** Store tz set to differ from server (e.g. America/Los_Angeles). One order placed late at night local time (e.g. 11:30pm local).
- **Steps:**
  1. As the customer, open /account/orders and find the late-night order's date/time.
  2. As admin, open /admin/reports Orders list and find the same order's timestamp.
  3. Open /admin/reports Clients list and check that customer's last-order date.
  4. Open the customer detail page /admin/customers/<id> and check firstSeen/lastOrder/lastLogin dates.
- **Expected:** Every one of these surfaces shows the timestamp in the RESTAURANT's local timezone and on the correct local calendar day — not shifted a day earlier/later by UTC. The account 'same-day' time, the Orders-list toLocaleString, the Clients-list last-order date, and the customer-detail dates all agree on the same local day.
- **Edges:** This is the crux of the tz diffs. Explicitly test the day-boundary crossing (an order near local midnight must NOT roll to the wrong day).

### 🔴 P0-ADMIN-SCOPING — Admin customer + reports pages still load and stay restaurant-scoped
- **Preconditions:** Admin logged in.
- **Steps:**
  1. Open /admin/customers/<id> for a customer of YOUR store — confirm it renders.
  2. Open /admin/reports/list/orders and /admin/reports/list/clients — confirm they render with data.
  3. Open /admin/reports/online-ordering/connectivity — confirm it renders the daily uptime list and device table.
  4. Confirm every row shown belongs to your restaurant only (no other store's customers/orders/devices).
- **Expected:** All four pages load without error and show ONLY the logged-in restaurant's data. The tz/formatDate edits and the deleted AssignPromotionForm did not break rendering or ownership scoping.
- **Edges:** Superadmin (restaurantId null) should not hit a redirect loop on these admin pages.

### 🟡 P1-CONNECTIVITY-DAYS — Connectivity report daily-uptime labels + device times render in store tz
- **Preconditions:** Admin; connectivity report has device history.
- **Steps:**
  1. Open /admin/reports/online-ordering/connectivity for a store in a non-UTC tz.
  2. Read the 'Daily uptime' list — each row's weekday/date label.
  3. Read the device table firstSeen/lastSeen columns.
- **Expected:** Each daily-uptime row is labelled with the correct LOCAL weekday and date (no off-by-one from UTC), and the day buckets align to local midnight→midnight. Device first/last-seen show in the store's timezone. The noon-anchored label must show the same day as the bucket it belongs to.
- **Edges:** Watch the first/last day of the selected range for boundary drift; watch a device seen near local midnight.

### 🟡 P1-DISH-COUNT-PILL — Category banner dish-count pill shows correct number and hides at 0
- **Preconditions:** Store with categories of differing sizes, ideally one empty category.
- **Steps:**
  1. Open /order/<slug>.
  2. For each category banner, read the fork/knife pill number in the lower-left.
  3. Compare it to the actual number of dishes listed under that category.
  4. Check an empty category (0 dishes) if present.
- **Expected:** Each pill shows the exact count of dishes in that category. A category with 0 items shows NO pill at all. The pill is icon+number only (language-neutral) so it reads correctly in any locale.
- **Edges:** 0-item edge: pill must be absent, not '0'. Long category names must still truncate without pushing the pill off-band.

### 🟡 P1-BANNER-HOVER-COLLAPSE — Category banner hover tint + accordion starts collapsed on desktop AND mobile
- **Preconditions:** A store with the accordion feature ON (theme.mobileCollapsibleCategories).
- **Steps:**
  1. On DESKTOP, open /order/<slug>. Note whether categories start collapsed.
  2. Hover over a collapsible banner — observe the darken tint.
  3. Hover over a top category-nav pill — observe it darkens ~10%.
  4. Click a banner to expand it, click again to collapse.
  5. Repeat on a MOBILE viewport (or narrow the window).
- **Expected:** With the accordion ON, ALL categories start COLLAPSED on both desktop and mobile (customer lands on a compact list of banners). Hovering a collapsible banner darkens the whole band; hovering a top nav pill darkens it slightly; cursor is a pointer. Expand/collapse toggling works. On a store with the accordion OFF, banners are NOT clickable and show no hover tint.
- **Edges:** Touch devices: hover tint should be a no-op (pointer-only). Theme colors: the tint/pill must respect theme.primaryColor and light/dark banner text, never hardcoded orange. RTL locales: pill and toggle button stay correctly positioned.

### 🟡 P1-THEME-RTL-LONGNAME — Order page banners honor theme color, RTL, and long names
- **Preconditions:** A store with a non-default theme.primaryColor; test in an RTL locale (e.g. Arabic/Hebrew) and with a very long category name.
- **Steps:**
  1. Set/choose a store with a distinct primaryColor and a long category name.
  2. Open /order/<slug> in an RTL locale.
  3. Inspect banner background, pill background, hover tint, and text contrast.
  4. Confirm the category title truncates and the pill remains visible/readable.
- **Expected:** Banner and pill use the store's theme color (no hardcoded orange on /order routes). Text stays legible on both light and dark banners. In RTL the layout mirrors correctly and controls don't overlap. Long names truncate with the pill still shown beneath.
- **Edges:** Explicitly the theme-color, RTL, and long-name edges.

### ⚪ P2-ASSIGNPROMO-REMOVED — Deleted AssignPromotionForm leaves no broken UI or missing-translation errors
- **Preconditions:** Admin.
- **Steps:**
  1. Open /admin/customers/<id> and interact with the promotions section.
  2. Watch for any React error, blank area, or raw i18n key text (e.g. an untranslated key showing instead of words).
  3. Switch the admin UI language to a couple non-English locales and reload the page.
- **Expected:** No console/runtime error, no orphaned 'Assign promotion' control, and no raw/missing translation keys in any locale — the dead component and its 38-locale keys were removed cleanly.
- **Edges:** Check at least 2 non-English locales to confirm i18n parity didn't break.

---

## E. FULL ON-DEVICE CHECKLIST

_Manual on-device test plan for commits cc4c98f0..012a0144 (order-page banner UI + coupon empty-cart fix + timezone rendering across account/reports + dead-code cleanup). Every case is copy-pasteable for a non-developer restaurant owner. Currently ALL 8 commits are validated by tsc/logic only — nothing has been verified on a real device yet._

**Setup:** GENERAL SETUP (do once):
1. Pick a test restaurant whose "Timezone" (Settings > restaurant profile) is set to a zone several hours away from UTC AND from your own — e.g. America/Los_Angeles (UTC-7/-8) or Pacific/Auckland (UTC+12/+13). This is what makes tz bugs visible; a UTC/near-UTC store will look "correct" even if broken.
2. Have at least one customer with: an order placed LATE AT NIGHT in the restaurant's local time (e.g. 11:30pm local, which is a DIFFERENT calendar day in UTC), an order from "today", one from "yesterday", one 3 days ago, and one older than 8 days (so the >7-day date branch renders). Easiest: place a real order now, then in the DB adjust createdAt on a copy to those offsets, OR just eyeball the single freshest order against a clock.
3. Have a promotion with a COUPON CODE and a MINIMUM ORDER set (e.g. code SAVE5, minimumOrder = $25). Assign it to a customer (or have its email "Use this offer" link) so you can arrive at the order page via that link with an EMPTY cart.
4. Have a restaurant with "Collapsible categories" enabled (theme.mobileCollapsibleCategories) AND several menu categories, at least one WITH dishes and — if possible — one category with ZERO dishes, plus one category with a very long name.
5. Devices: one PHONE (real touch device) and one DESKTOP browser with a mouse. Test the order page on BOTH.
6. If possible also set restaurant locale to an RTL language (Arabic/Hebrew) for the RTL edge check on the banner pill.

**Untested surface in this area:**
- Coupon route empty-cart acceptance (subtotal===0) — validated by logic/tsc only; NOT verified live that the code registers and the 'not applied' message can now appear.
- Coupon route: real money gate — NOT verified live that a sub-minimum non-empty cart still fails to receive the discount at final order placement (apply-promos + order route). This is the P0 money risk.
- account/orders/page.tsx formatDate(tz) — tz rendering of same-day time and >7-day date; late-night/DST correctness NOT verified on device.
- admin/customers/[id]/page.tsx firstSeen/lastOrder/lastLogin in tz — NOT verified live; also the no-timezone ({}) fallback path.
- admin/reports/list/clients formatLastOrder(tz) — late-night calendar-day rollover NOT verified live.
- admin/reports/list/orders createdAt toLocaleString(tz) — NOT verified live; cross-surface consistency with other reports unverified.
- connectivity report: eachDayKeyInTz bucketing + parseLocalDateTimeInTz day boundaries + noon-anchored labels + device first/last-seen tz — the most complex tz change; day-count-at-range-edges, offset-rollover, and DST correctness NOT verified live.
- OrderingPageClient dish-count pill — count accuracy, 0-item hide, theme-color legibility, RTL placement NOT verified on device.
- OrderingPageClient hover tint on banners — desktop darken + touch no-op + click-through (pointer-events-none) NOT verified on device.
- OrderingPageClient top pill hover:brightness-90 + cursor + scroll-on-click NOT verified on device.
- Collapsible accordion seed change (all-collapsed on desktop now) + seededRef guard across breakpoint/search — NOT verified on device; this is a visible behavior change for existing desktop users.
- AssignPromotionForm deletion — no-refs confirmed statically (only a comment mention remains in scripts/i18n-reword-coupon-to-offer.ts); build/runtime/i18n-parity NOT re-verified via preflight + all-38 audit.

### 🔴 TC-COUPON-01 — Empty-cart coupon via 'Use this offer' link no longer throws a spurious minimum-order error (P0 money/correctness)
- **Preconditions:** Promotion with couponCode=SAVE5 and minimumOrder=$25, assigned to a customer or reachable via its email 'Use this offer' link. Cart is EMPTY.
- **Steps:**
  1. Log in as the customer (or open the email).
  2. Click the promotion's 'Use this offer' button / email link so it lands you on the restaurant order page BEFORE you add anything to the cart.
  3. Do NOT add any items yet — keep the cart empty.
  4. Observe: the code should be accepted/registered silently. There must be NO red 'Minimum order of $25 required for this promo' error popping up on arrival.
  5. Now add a single cheap item so the subtotal is BELOW $25 (e.g. $8).
  6. Proceed toward checkout / view the cart promo area.
- **Expected:** On arrival with an empty cart: coupon is accepted, no minimum-order error is shown, and the coupon is registered so the cart can later show the 'not applied — a bigger deal won' style message. Once items are added but subtotal is still under $25, the minimum IS enforced (either the 'bigger deal won'/not-applied message or, at the order route, the discount does not apply). Money correctness: an under-minimum order must NOT actually receive the discount.
- **Edges:** Regression check: subtotal exactly 0 = accept; subtotal >0 but <minimum = still rejected/deferred; subtotal >= minimum = applies. Verify the discount does NOT silently apply on a sub-minimum cart at final order placement (the real money gate is apply-promos + order route, not this GET).

### 🔴 TC-COUPON-02 — Coupon with a minimum still correctly blocks/does-not-apply on a non-empty under-minimum cart (P0 regression)
- **Preconditions:** Same SAVE5 / $25 minimum promo. Start with items already in cart totalling under $25 (e.g. $10).
- **Steps:**
  1. Add items to reach a subtotal under the $25 minimum.
  2. Enter the coupon code SAVE5 manually in the cart's code box.
  3. Observe the response.
- **Expected:** The minimum is still enforced for a real (non-empty) cart: the code is rejected with the 'Minimum order of $25 required' message OR shows as not-applied. The discount must NOT reduce the total. This confirms the fix only relaxed the subtotal===0 case, not the real enforcement path.
- **Edges:** This is the guardrail proving the change didn't open a hole where any under-minimum cart gets the discount.

### 🔴 TC-TZ-01 — Customer 'My Orders' list shows late-night order time/date in the RESTAURANT timezone, not server UTC (P0 correctness)
- **Preconditions:** Restaurant tz far from UTC (e.g. America/Los_Angeles). Logged in as a customer with a same-day order placed late at night local time, plus a >8-day-old order.
- **Steps:**
  1. Log in as the customer and open the account Orders page (/account/orders).
  2. Find the order placed late at night in the restaurant's local time (today) and read the time shown next to the status pill.
  3. Compare that time to the actual local wall-clock time the order was placed in the restaurant's city.
  4. Also look at an order older than 7 days and read its month/day.
  5. Cross-check the same order's timestamp on the admin Orders report for that restaurant.
- **Expected:** Same-day order shows the restaurant-local time (e.g. '11:34 PM'), matching the restaurant's clock — NOT a UTC-shifted time. The >7-day order's month/day is the restaurant-local calendar date. 'Yesterday' and 'N days ago' buckets are unaffected (they compare absolute instants) and should read the same as before.
- **Edges:** LATE-NIGHT edge is the whole point: a 11:30pm-local order is a different UTC day; before the fix it could show the wrong hour or roll to the next day. DST edge: pick a date on the other side of a DST change if the store's tz observes it and confirm the hour is still right.

### 🟡 TC-TZ-02 — Customer detail page (admin) firstSeen / lastOrder / lastLogin render in restaurant tz (P1)
- **Preconditions:** Admin logged into a store with a far-from-UTC tz. A customer with lastOrderAt late-at-night local and a lastLoginAt.
- **Steps:**
  1. Go to Admin > Customers > open a customer whose activity happened late at night local time.
  2. Read the 'First seen', 'Last order', and 'Last login' dates in the info grid.
- **Expected:** All three dates reflect the restaurant's local calendar day/time (using formatDate(date, tzOpts) from @/lib/utils). A late-night lastOrder should not show tomorrow's date. If the restaurant has no timezone set, tzOpts is {} and it falls back to prior UTC behavior (no crash).
- **Edges:** Confirm the couponExpires line and any hour:minute render (which already used tzOpts) still look consistent with the three newly-converted lines.

### 🟡 TC-TZ-03 — Clients report 'Last order' column uses restaurant-local calendar day (P1)
- **Preconditions:** Admin on a far-from-UTC store. A customer whose most recent order was placed near midnight local time.
- **Steps:**
  1. Go to Admin > Reports > Clients list.
  2. Find the customer with the near-midnight last order.
  3. Read the 'Last order' date cell.
- **Expected:** The date is the restaurant-local calendar day of that order (scope.timezone passed through). A 11:50pm-local order does not display as the next day. Customers with no last order still show '—'.
- **Edges:** Late-night / calendar-day-rollover edge. Verify against the same order in the Orders report (TC-TZ-04) — the two dates should agree.

### 🟡 TC-TZ-04 — Orders report createdAt column renders in restaurant tz (P1)
- **Preconditions:** Admin on a far-from-UTC store with a late-night order.
- **Steps:**
  1. Go to Admin > Reports > Orders list.
  2. Read the created-at/date-time cell for the late-night order.
  3. Compare with the restaurant's local clock and with the same order in the Clients report and customer Orders page.
- **Expected:** createdAt shows restaurant-local date+time (toLocaleString with scope.timezone). Consistent across Orders report, Clients report, and customer /account/orders for the same order.
- **Edges:** If scope.timezone is unset it uses server default ({}), no crash. Cross-surface consistency is the real assertion here.

### 🟡 TC-TZ-05 — Connectivity/uptime report buckets and labels by restaurant-local days + device times in tz (P1)
- **Preconditions:** Admin on a far-from-UTC store that has (or had) at least one kitchen device reporting connectivity over several days. Ideally a device whose first/last seen was late at night local.
- **Steps:**
  1. Go to Admin > Reports > Online Ordering > Connectivity (uptime).
  2. Look at the 'Daily uptime' list: each row's weekday/date label.
  3. Confirm the number of day rows matches the selected date range's LOCAL days (no missing or extra day at the range edges).
  4. In the device table, read First seen / Last seen for a device.
- **Expected:** Each daily row is labelled with the restaurant-local calendar day (weekday, month, day) and buckets uptime midnight-to-midnight in the restaurant's tz — the day boundaries and labels do NOT drift by a day for an out-of-tz store. Device First/Last seen show restaurant-local date+time. The day count spans exactly the selected local range.
- **Edges:** OFFSET-ROLLOVER edge: labels are noon-anchored (new Date(`${dayKey}T12:00:00`)) so a store at UTC+13/UTC-12 must NOT show the label off by one day vs the bucket. DST edge: a range crossing a DST boundary should still produce the right number of days with correct labels. Compare first/last-seen day here to the same device's activity elsewhere.

### 🟡 TC-BANNER-01 — Category banner dish-count pill shows correct number, hides on empty categories, respects theme colors + RTL (P1)
- **Preconditions:** Order page on a store with several categories: at least one with a known dish count, ideally one with 0 dishes, one with a long name. Test on both light and dark banner backgrounds if the store has category images.
- **Steps:**
  1. Open the customer order page (/order/<slug>).
  2. For a category you know has N dishes, find its banner and read the small pill (fork/knife icon + number) in the lower-left under the category title.
  3. Verify the number equals the actual number of dishes in that category.
  4. Find a category with ZERO dishes and confirm NO pill is rendered.
  5. Find a category with a very long name and confirm the title truncates and the pill still sits correctly below it.
  6. Switch the store locale to an RTL language (Arabic/Hebrew) and re-check the pill placement/legibility.
- **Expected:** Pill shows the exact menuItems.length count with a fork/knife (Utensils) icon; it is language-neutral (no translated word, so nothing to break across the 38 locales). No pill on 0-item categories. Pill background/text use the theme-derived overlay colors (readable on both image and solid-color banners). In RTL the pill remains legible and reasonably placed. No console errors.
- **Edges:** 0-ITEMS edge (no pill), LONG-NAME edge (title truncates, pill unaffected), THEME-COLOR edge (readable on light vs dark banner via overlayText / rgba backgrounds), RTL edge. Utensils was already imported — confirm no build/runtime import error.

### ⚪ TC-BANNER-02 — Hover tint darkens a clickable/collapsible banner on desktop; no-op on touch (P2)
- **Preconditions:** Store with collapsible categories ENABLED. Desktop browser with a mouse AND a phone.
- **Steps:**
  1. On DESKTOP, open the order page for the collapsible-enabled store.
  2. Hover the mouse over a category banner (which is clickable to collapse/expand).
  3. Observe the band darkens slightly (a ~20% black tint) while hovered, and returns to normal when the mouse leaves.
  4. Click it and confirm it still toggles collapse/expand (hover overlay is pointer-events-none and must not block the click).
  5. On PHONE, tap the same banner and confirm normal collapse/expand with no stuck dark overlay.
- **Expected:** Desktop: smooth darken-on-hover only while hovered; click still toggles the category. Touch: no hover tint sticks; tapping toggles as before. When categories are NOT collapsible, banners are not clickable and the hover tint does not appear.
- **Edges:** pointer-events-none must not swallow clicks; verify carousel arrows (if any) still work and aren't double-tinted.

### ⚪ TC-BANNER-03 — Top category-pill nav buttons darken on hover and show pointer cursor over theme colors (P2)
- **Preconditions:** Order page with the sticky top category-pill nav. Desktop with mouse.
- **Steps:**
  1. On desktop, hover over the top category pill buttons (the horizontal quick-jump chips).
  2. Observe each pill darkens ~10% (brightness filter) on hover and the cursor is a pointer.
  3. Click a pill and confirm it still scrolls to that category.
  4. Check both the ACTIVE pill (theme.primaryColor bg) and inactive pills (card background).
- **Expected:** Hover darkens both active and inactive pills via brightness filter (works over inline theme colors), cursor is pointer, and click still scrolls to the category. No color glitch on the active vs inactive states.
- **Edges:** THEME-COLOR edge: brightness filter must look right on both the primaryColor active pill and the card-background inactive pills for stores with unusual theme colors.

### 🟡 TC-BANNER-04 — Collapsible categories now seed ALL COLLAPSED on desktop AND mobile on first load (P1 behavior change)
- **Preconditions:** Store with collapsible categories ENABLED and multiple categories.
- **Steps:**
  1. On DESKTOP (fresh load, not after searching), open the order page.
  2. Confirm every category starts COLLAPSED — you land on a compact stack of category banners, not a fully expanded menu (this is the NEW behavior; desktop previously started expanded).
  3. Click a banner to expand one category and confirm items appear.
  4. On MOBILE, repeat: confirm all categories start collapsed.
  5. Use 'Expand all' / 'Collapse all' controls and confirm they work.
  6. Type in the menu SEARCH box and confirm collapsing is suspended so matching items are visible even in collapsed categories.
  7. Clear the search and confirm your manual open/closed choices are preserved (not re-seeded).
- **Expected:** On BOTH desktop and mobile, first load = all categories collapsed. Expand/Collapse all works. Search overrides collapse to reveal matches. After the first seed, the collapseSeededRef guard prevents re-seeding, so a breakpoint change (rotate phone / resize) or clearing search does NOT wipe the customer's manual expand/collapse choices.
- **Edges:** Resize/rotate across the mobile/desktop breakpoint must NOT re-collapse everything (seededRef guard). Only-when-collapsible: a store WITHOUT the feature enabled should still show the normal fully-visible menu.

### 🟡 TC-CLEANUP-01 — Removed AssignPromotionForm dead code + its 38-locale i18n keys cause no regressions (P1)
- **Preconditions:** Admin app builds/runs.
- **Steps:**
  1. Confirm the app builds (npm run preflight passes — tsc + prisma + next build, read bottom-up).
  2. Open Admin > Customers > a customer detail page and interact with the remaining promo/assign UI (the current promotions overhaul flow, e.g. assign a visible promo + email).
  3. Load a few pages in a non-English locale to confirm no missing-translation console warnings from the removed keys.
- **Expected:** Build is clean. No page references the deleted /api/admin/promotions/assign-to-customer form. No 'missing message' errors in any of the 38 locales for the removed keys. The current assign-promotion-to-customer flow (post-overhaul) still works end-to-end.
- **Edges:** i18n PARITY edge: 27 lines removed identically across all 38 locale files — run the all-38 parity audit (0 missing/extra/placeholder/rich-tag mismatch) to be sure no file was left inconsistent.

---

## F. CRITIC-ADDED CASES (coverage gaps the 5 authors missed)

**Coverage gaps identified:**
- Coupon min-order fix (src/app/api/public/coupon/route.ts): the new `subtotal > 0` guard is a payments/checkout hot-path change and has ZERO test cases. The entire plan is timezone-only. Nothing verifies (a) an empty-cart 'Use this offer' / email-link redemption now succeeds instead of throwing a spurious 'Minimum order' error, NOR (b) the critical safety property that a below-minimum discount does NOT actually apply at checkout — promo-engine.ts:312 still enforces `subtotal < minimumOrder` in isEligible(), but no case confirms the engine catches what the route now waves through. A regression here either re-breaks the 'a bigger deal won' cart message or silently grants a discount below the minimum.
- Category dish-count pill (src/app/order/[slug]/OrderingPageClient.tsx, the headline final commit 012a0144): no case renders the pill. Untested: the count matches visible menuItems.length, the pill is hidden for an empty category (`cat.menuItems.length > 0`), the Utensils icon renders (broken import = customer-order-page crash on a hot path), and the pill's contrast styling on both image-banner and flat-color (light vs dark) categories.
- Collapse-all now defaults COLLAPSED on desktop too (was mobile-only; net effect of 167610bd/fd110cd1 + the seed-effect change): desktop customers with theme.mobileCollapsibleCategories now land on a fully-collapsed accordion. No case covers this. Untested: (a) the SSR/hydration flash — server renders desktop expanded (no isMobile on server), client effect then collapses everything, a visible flicker on the customer ordering hot path (matches verified finding collapse-seed-desktop-flash); (b) Expand all / Collapse all still work on desktop; (c) starting a search still suspends collapse so matching items are visible (collapsibleActive gates on !menuSearchQuery).
- Category-pill and banner hover-darken (hover:brightness-90 on top pills; group-hover:bg-black/20 on banners): purely visual, no case. Untested that hover tint only applies to clickable/collapsible banners (carousel-arrow banners excluded via the `collapsible &&` guard) and is a no-op on touch.
- Orphaned AssignPromotionForm removal + deletion of its i18n keys across all 38 locales (939e83ef): no case confirms nothing still imports/renders AssignPromotionForm (verified finding orphaned-assign-route rated NOT real, but a build/preflight smoke check is still absent from the plan) and no case confirms the 38-locale parity audit passes after the bulk key deletion (0 missing/extra) — required by the standing i18n rule.
- List Orders / Customer-detail / Connectivity-device-times use toLocaleString(undefined, {timeZone}) i.e. the SYSTEM locale, not 'en-US'. The plan asserts specific en-US formats (e.g. '7/1/2026', '11:30 PM') but the locale is undefined, so the month/day order and 12/24h clock depend on the server locale — no case pins down what format actually renders vs the plan's assumed strings.
- Connectivity daily-uptime LABEL uses `new Date(`${dayKey}T12:00:00`).toLocaleDateString(...)` with NO timeZone option — noon-anchored in server-local. Combined with the verified conn-dst-day-bucket finding, no case covers a DST-transition day (23h or 25h local day) where the fixed DAY_MS bucket math can over/under-count uptime % or mislabel the bar. The plan's DST setup (item D) exists but no testCase actually asserts the DST-day bar count/label/percentage.
- Connectivity CSV export (…/export route) was NOT in this diff and still emits UTC — the plan's untestedSurface flags this but provides no case to confirm the divergence with the in-page (now-local) dates, so the reseller/owner sees mismatched dates between screen and download (verified finding tz-connectivity-export-divergence, real/low).

### 🔴 CPN-P0-01 — Coupon accepted on an EMPTY cart via account/email link (min-order deferred, not rejected)
- **Steps:**
  1. Create a promo with minimumOrder set (e.g. $25) and give it a redemption code.
  2. As a signed-in customer with an empty cart, hit the redemption path that calls GET /api/public/coupon (the account 'Use this offer' button or the email link) BEFORE adding any items.
  3. Observe the response and the cart-message state.
- **Expected:** The code is accepted (200, no error). NO 'Minimum order of $25 required' error fires at subtotal 0. The coupon registers so the 'not applied — a bigger deal won' message can later show. Before the fix this returned a 400 and the client threw.

### 🔴 CPN-P0-02 — Below-minimum discount does NOT apply at checkout even though the code was accepted empty
- **Steps:**
  1. With the same $25-minimum promo accepted on an empty cart (per CPN-P0-01), add items totalling only $10.
  2. Proceed toward checkout / trigger apply-promos and place the order via POST /api/orders.
- **Expected:** The promo is NOT applied to the $10 order — promo-engine isEligible() blocks it (subtotal $10 < $25). No discount line appears and the order total is full price. This is the safety net the route now relies on; a regression here would grant a below-minimum discount.

### 🟡 CPN-P1-03 — Above-minimum cart still applies the discount normally (no regression to the happy path)
- **Steps:**
  1. With the $25-minimum promo, add items totalling $40.
  2. Apply the code and place the order.
- **Expected:** Discount applies as before. The `subtotal > 0 && subtotal < minimumOrder` guard only changes the empty-cart branch; a non-empty above-minimum cart is unaffected.

### 🟡 ORD-P1-04 — Category dish-count pill shows the correct count and hides for empty categories
- **Steps:**
  1. Open /order/<slug> for a restaurant with collapsible categories enabled.
  2. Read the pill (fork/knife icon + number) on each category banner.
  3. Include a category that has visible items and one with zero visible items.
- **Expected:** Each pill number equals that category's visible menuItems.length. A category with 0 items shows NO pill. The Utensils icon renders (page does not crash — a missing import would white-screen the customer order page).

### 🟡 ORD-P1-05 — Dish-count pill is legible on both image-banner and flat-color categories (light + dark)
- **Steps:**
  1. On /order/<slug>, view a category WITH a banner image, one WITHOUT (flat theme.primaryColor), on both a light and a dark theme.
- **Expected:** Pill background/text contrast is readable in all four combinations (image, dark-flat use rgba(255,255,255,0.20); light-flat uses rgba(0,0,0,0.10); text = overlayText). No white-on-white or black-on-black.

### 🔴 ORD-P0-06 — Desktop with collapsible categories starts fully COLLAPSED without a jarring expand→collapse flash
- **Steps:**
  1. On a DESKTOP viewport, load /order/<slug> for a restaurant with theme.mobileCollapsibleCategories enabled.
  2. Watch the first paint through hydration.
- **Expected:** Categories settle collapsed (compact list of banners). Note any visible flash where the menu renders fully expanded on SSR then snaps collapsed on the client (known low-sev hydration flash) — confirm it is acceptable to Luigi or needs an SSR-side collapsed default.

### 🟡 ORD-P1-07 — Expand all / Collapse all and search-suspend still work on desktop after the collapse-default change
- **Steps:**
  1. On desktop /order/<slug> (collapsible on, now starting collapsed), click 'Expand all', then 'Collapse all'.
  2. Type a query into the menu search.
- **Expected:** Expand all opens every category; Collapse all closes them. Starting a search suspends the accordion (collapsibleActive=false) so matching items are visible regardless of prior collapsed state; clearing the search restores the customer's open/closed choices (seededRef not re-seeded).

### 🔴 I18N-P0-08 — 38-locale parity + build after AssignPromotionForm removal and its key deletions
- **Steps:**
  1. Run npm run preflight (tsc + prisma generate + full next build).
  2. Run the all-38 i18n parity audit (0 missing / extra / placeholder-arg / rich-tag mismatch).
  3. Grep the tree for any remaining import of AssignPromotionForm.
- **Expected:** Build passes (read bottom-up for 'Build error occurred'). Parity audit is clean across all 38 locales after the bulk deletion of the assign-promo keys. No dangling import references the deleted component.

### 🟡 TZ-P1-09 — System-locale format assumption on List Orders / Customer-detail / Connectivity device times
- **Steps:**
  1. On the server/deployment under test, open List Orders and read a date cell (toLocaleString(undefined, {timeZone})).
  2. Compare the rendered month/day order and 12/24h clock against the plan's assumed 'en-US' strings (e.g. '7/1/2026, 11:30 PM').
- **Expected:** Confirm whether the undefined-locale output actually matches the plan's en-US assumptions on the target server locale. If the server locale differs, the format assertions in TZ-P0-01/02/04/05 need adjusting — the fix pins the timeZone but NOT the locale.

### 🟡 TZ-P1-10 — Connectivity DST-day uptime bar: count, label, and percentage on a spring-forward / fall-back local day
- **Steps:**
  1. On the West (Los_Angeles) store, seed device/connectivity intervals spanning the DST transition (setup item D: 2026-03-08 spring-forward and 2026-11-01 fall-back).
  2. Open the Connectivity report over a range covering the transition day.
  3. Read the number of bars, the transition-day label, and its uptime %.
- **Expected:** Exactly one bar per local calendar day (the 23h/25h DST day is still one bar). The label reads the correct local weekday/date (noon-anchor). Flag that DAY_MS is a fixed 86.4M ms while the local DST day is 23h/25h — verify the % is not skewed >100%-clamped or artificially low on that one day (known low-sev conn-dst-day-bucket).

### ⚪ TZ-P2-11 — Connectivity CSV export dates vs in-page (screen now local, export still UTC)
- **Steps:**
  1. On a store in a non-UTC tz, note the dates shown in the in-page Connectivity report.
  2. Download the Connectivity CSV export (…/export route — NOT in this diff) and compare the date columns.
- **Expected:** Document the divergence: in-page dates are now the restaurant-local day, but the export route still emits UTC. Confirm with Luigi whether the export must be brought in-tz too before sign-off (verified tz-connectivity-export-divergence, low).

### 🟡 TZ-P1-12 — Null-timezone restaurant does not crash and falls back cleanly on all six surfaces
- **Steps:**
  1. Use a restaurant with timezone unset (null).
  2. Visit List Orders, List Clients (last-order), Customer detail (3 stats + order history), /account/orders, /account recent orders, and Connectivity.
- **Expected:** No crash. Each formatter falls back to server/local formatting (tzOpts = {} when timezone is null). Dates render, just without a tz override.
