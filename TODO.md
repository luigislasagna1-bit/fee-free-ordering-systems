# TODO — Luigi's running to-do list

Every note Luigi flags — "add this to the to-do list", "still left", "note —", an idea in passing — lands
here and gets done at some point. **Newest at the top of Open.** When it ships, move it to **Done** with the
date + commit hash. This file is committed so the backlog never gets lost.

> Standing rule (Luigi, 2026-06-24): _"Anything I put in as this kind of note needs to be added to our
> to-do list and get done at some point."_ → Always capture it here first.

## Open

- [x] **✅ SHIPPED (2026-07-02, 651dbd75) — Split "can't pay with Pizza Bucks" from "no promo discounts" (Luigi 2026-07-02).** Luigi (during the launch checkout test): bucks-redeemability and promo-discountability must be SEPARATE per-item/category choices with their own picker. Built: `rewardRedeemExcluded` on MenuItem+MenuCategory (BOTH branches, backfilled from promoExcluded via `scripts/backfill-redeem-excluded.ts` — 1 prod category); promoExcluded now = discounts only; redeem base (reserveCredit + preview `redeemExcludedTotal`) keys on the new flag; new "Items that can't be paid with {label}" panel on /admin/rewards; all three exclusion editors now share the collapsible `MenuExclusionsPanel` (chevron + excluded-count badge — the "minimize/maximize" ask); 5 strings ×38, parity clean. **Verify on prod:** gift-card cart behaves the same as before the split (backfilled); untick "No discounts" while keeping "Not payable" → promo applies to the gift card but the Bucks cap stays reduced.

- [x] **✅ Ghost-ring kitchen test #18 CONFIRMED on-device (Luigi 2026-07-01).** The "logged-out kitchen device must NOT ring" fix (16b77b18 — retire push tokens on login + freshness-gate the poll) is verified working on real hardware. This was the MOST IMPORTANT kitchen test + a 🔴 launch blocker — now cleared. See [[reference_kitchen_ring_session_lifecycle]].

- [ ] **🔴 Reseller attribution — a "direct" signup got credited to a reseller via a STALE 30-day cookie (Luigi 2026-07-01).** Luigi signed up dilpreetsinging@gmail.com "directly" at feefreeordering.com but the welcome email said "your Fee Free partner is PISU MARKETING" (Fabrizio). ROOT CAUSE (confirmed, not a logic bug): `src/app/api/auth/register/route.ts:83-86` resolves the reseller from `ref` (URL) OR the `feefree_ref` COOKIE; that cookie is a 30-day last-touch cookie set at `src/app/signup/SignupForm.tsx:98` whenever the browser hits `/signup?ref=<code>` or a reseller's branded host. Luigi's earlier reseller testing planted Fabrizio's code in that browser, so the later direct signup inherited it. **Was working as designed (30-day last-touch), but the design surprised.** ✅ FIXED 2026-07-01 (Luigi chose "shorten the window"): the `feefree_ref` cookie is now a SESSION cookie (no max-age) at `SignupForm.tsx:98` — attribution survives navigating away + back within the same visit, but no longer lingers for weeks to catch unrelated later "direct" signups. Preflight green. **Still Luigi's to do — immediate remediation of the ALREADY-mis-attributed test account:** (1) fix the mis-attributed test account — superadmin "assign reseller" tool (retro-fix, set reseller to none) per [[project_reseller_referral_attribution]], or just delete the test account; (2) to re-test cleanly, clear the `feefree_ref` cookie or use an incognito window → should then show no partner. Note: real owners who click a reseller link once then sign up "directly" weeks later are currently credited to that reseller (commission leakage) — this is why B matters at scale. (Luigi 2026-07-01).** Luigi thought a new signup didn't get its verification email, but likely mistyped the email at signup (→ went to the wrong address / bounced), so probably NOT a real bug. To confirm: do one clean signup with a correct address you control and check it arrives (remember emails only really send from PROD). Only if it genuinely fails on a correct address, investigate the signup → send-verification path (`src/app/api/auth/register/route.ts` / `src/app/api/restaurants/[slug]/account/signup/route.ts` → the verify-email send; look for a swallowed error or skipped call). Low priority unless it reproduces.
- [ ] **🟠 MONEY-DISPLAY CONSISTENCY everywhere (Luigi 2026-07-02) — IN PROGRESS.** ✅ DONE: shared `buildMoneyBreakdown()` + `paymentMethodLabelKey()` helpers + 7 tests (a7b2f396); `money.*` labels ×38 (abb61fb7); kitchen order detail = Pizza Bucks used + COLLECTED (b5cd0b65); confirmation = tip + balance + payment method (06943ca5, 377c85d5); admin order detail = reward used/collected/earned + payment label (377c85d5); reports ACCOUNTING seam = online/offline collected now net of store credit + storeCreditRedeemed/collected in DigestStats (6c44ee49). **🔜 REMAINING:** ~~(1) EMAILS~~ ✅ DONE 2026-07-02 (1bc95394, found live by Luigi on ORD-462443388): customer OrderConfirmation now gets the full breakdown (subtotal/tax/tip/discount threaded — previously "Subtotal" silently showed the TOTAL) + "Paid with {label}" + "Balance to pay"/"Paid" + localized payment-method line (all row labels now localized via existing receipt.customer.* keys); staff KitchenNotification (new-order + accepted) shows "Paid with X" + "To collect"/"Collected" (accepted email led with the misleading gross total before); kitchen push body now shows total − credit. Feature-gated on rewardsEnabled per the new standing rule. Kitchen/staff OrderItemsTable headers (Qty/Items/Price/Note) still hardcoded English — part of the remaining email-i18n Medium item. ~~(2)–(7)~~ ✅ ALL DONE 2026-07-04 (b0242876 + the reports-polish commit after it): (2) status page = balance/paid row + localized payment method; (3) EOD slip + kitchen EOD modal + /admin/reports/end-of-day = "Discounts" line + "Store credit −X" + bold COLLECTED (rows only when nonzero); Sales Summary + its CSV/XLS export = Discounts column always, Store credit + Collected columns data-gated on credit redeemed in range; (4) kitchen tile = small "To collect: X" line ONLY on store-credit part-paid unpaid orders (locked layout otherwise untouched — worth an on-device glance); (5) ShipDay `totalOrderCost` = total − creditApplied (⚠️ still to VERIFY on one live ShipDay order — push body was already fixed in 1bc95394); (6) Promotions report now groups by each order's appliedPromos snapshot via shared `buildPromoStatRows` (page + export; legacy couponId-only orders still roll up) — was showing ONLY retired coupons; /admin dashboard headline cards use the canonical REPORT_ORDER_STATUS_WHERE (numbers may shift — they previously counted rejected/cancelled/TEST); Brand dashboard "today" = local midnight per location tz (was UTC) + same canonical filter; customer-detail Orders/Spent recomputed from orders (stored counters drift high when orders get rejected; list page keeps counters to avoid N+1); (7) reservation deposit hint via formatCurrency(restaurant.currency). New i18n key money.discounts ×38. **STILL OPEN from (1):** kitchen/staff OrderItemsTable headers (Qty/Items/Price/Note) hardcoded English — part of the email-i18n Medium item. Original symptoms below. The kitchen display doesn't show the Pizza Bucks (reward credit) breakdown; the customer confirmation buries the TIP inside Total and omits payment method + balance/paid. AUDIT + normalize EVERY place an order's money shows — kitchen display (tile + detail + EOD), physical receipt (GOLDEN — verify only, Luigi says it's good), customer confirmation + status + account, ALL emails (customer confirmation, kitchen/staff notification, per-type notification emails), admin order detail, reports/EOD — so each shows the SAME accurate, labeled breakdown: line items → Subtotal → each Discount/promo (name+code) → Delivery fee → other fees → Tax → Tip → TOTAL → Reward/store-credit used (−) → AMOUNT PAID/TO PAY + how (Cash/Card ••••/PayPal) → Reward EARNED (+) → PAID vs balance-remaining. Never bury the tip or omit the credit-used / payment method. Audit running 2026-07-02.


- [x] **✅ FIXED (2026-07-02) — 🟠 MONEY: cart PREVIEW ≠ CHARGE (launch Blocker #7).** All 4 divergences closed structurally: both routes now build their promo pool + customer identity through ONE shared `buildPromoOrderContext()` in `src/lib/promo-order-context.ts` (pool incl. parent brand-scope + take:500; canonical member = session OR CustomerAccount-by-email; new/returning = fulfilled orders by id OR email OR phone in-channel; lifetime = ledger + customerId history; grant/VIP add-backs identical) — preview == charge by construction. Client `isMember` flag no longer trusted (server derives from session). 6 parity tests in `promo-order-context.test.ts` assert previewed == charged cents for: member-only cart (signed-in, no CustomerAccount — the charge-MORE bug), guest member-off, child-of-brand cart (brand promo in both, parent own-promos never leak), lifetime-via-customerId history, phone-only returning guest, failed-orders-stay-new. 341 vitest green, preflight exit 0. Original trace below:
  1. **Member signal (charge-MORE risk — do first).** Preview `isMember = !!currentCustomer` (restaurant-customer SESSION; `OrderingPageClient.tsx` ~L2144) vs charge `isMemberForPromo` = a marketplace `CustomerAccount` exists for the typed email (`orders/route.ts` ~L1355-1359). A signed-in restaurant-customer with no CustomerAccount PREVIEWS a member-only promo then is CHARGED full. Pick ONE canonical member definition and use it in both. Bites `customerType==="member"` promos (`promo-engine.ts` isEligible ~L345).
  2. **Brand/franchise scope (charge-LESS).** Preview loads only own promos (`apply-promos` L49-52: `{restaurantId, isActive}`); charge ALSO loads parent `scope:"brand"` promos (`orders` L1176-1190 via `promoOwnerIds`). A CHILD-store customer sees no brand promo in preview but gets it at charge. Mirror the orders promo pool (add parentRestaurantId + the brand-scope OR) into apply-promos.
  3. **once-per-lifetime identity scope.** Preview `usedLifetimePromoIds` passes only {email,phone} and is skipped with no identity (`apply-promos` ~L224-237); charge also passes customerId + scans order history (`orders` ~L1448-1458, `coupon-ledger.ts` L261-285). Anonymous/early preview shows a spent lifetime promo the charge refuses — but RECONCILES once the customer types their email (before placement), so low real-total risk. Align by passing customerId in preview when signed in.
  4. **new/returning for phone-only guest.** Preview keys new/returning on email OR phone (`apply-promos` L80-94); charge keys `isNewCustomer` on the email→Customer row only (`orders` ~L1305-1340). A phone-only returning guest can flip customerType. Make both use the same identity keys.
  (Minor/UX, no fix needed: the ORDER route doesn't hard-reject a bogus/exhausted couponCode at charge — the engine just yields $0, never over-discounts; rejection is enforced at the `/api/public/coupon` apply-gate. Server never OVER-charges from a bad code.)

- [ ] **🔴 GO-LIVE: platform Stripe account must be "Fee Free Ordering Inc." (Luigi 2026-07-02).** During card-save testing the Stripe Checkout page + card-statement descriptor showed **"Luigis Lasagna & Pizzeria Inc." (Sandbox)** — because `PlatformSettings` Stripe keys (Superadmin → Stripe Settings) currently point to Luigi's own/test Stripe account. This MISMATCHES the invoice issuer we now render ("Fee Free Ordering Inc."). For merchant-of-record consistency the three must all agree: **invoice issuer ↔ Stripe Checkout page ↔ card-statement descriptor = Fee Free Ordering Inc.** FIX (no code — Stripe Dashboard + settings): create a Stripe account under the Fee Free legal entity, set its business name + statement descriptor to "Fee Free Ordering", then paste its **LIVE** keys into Superadmin → Stripe Settings before launch. Until then everything is test-mode/sandbox (fine for testing; not launch-ready).

- [x] **DONE (2026-07-02, 29fccf99) — Auto-geocode the restaurant address by default on /admin/profile (Luigi).** LocationSection now geocodes the entered address automatically (debounced 1s) and drops the pin — no button click needed; the owner can still drag the pin. Seeds on mount (existing saved pins survive until the address is edited), skips the autocomplete-picked address, and the manual "Geocode from address fields" button stays as a fallback. Also routed the profile autocomplete street through the country-aware composeStreetLine.

- [ ] **Reseller-reports FIX BACKLOG (completeness review 2026-07-01/02, Fabrizio).** A per-report code-verified pass split the open reports. **✅ COMPLETE + ready for Fabrizio to verify/close (send the verify email from the Reseller Reports UI):** Report/dashboard calculations (cmqsoloe6), Coupon BUG/HIGH (cmqtllluu), Which-dishes-discounted (cmqv33v2o), Promo dish-categories multi-menu (cmqv36jr0), Empty cart (cmqwkzm0c), **Promo "Get it Now" (cmqtmfp2n) — redundant bottom button removed 2026-07-02 (c7e938cf)**, **Delivery/pickup timeframes (cmqt99i8s) — ASAP-delivery estimate clarified at checkout, 38 locales, 2026-07-02 (c7e938cf)**. **✅ NOW ALSO DONE (2026-07-02, 70f6cd56) — the 2 coupled special-day-hours reports:** (1) _Closing days / closed services_ (cmqp8l948): special OPEN hours for a single service (e.g. delivery 10:00–20:00 today) now take precedence in the checkout time-slot picker + earliest slot + per-service open-now status. (2) _Opening hours per service_ (cmqnm3hv0): distinct "🕒 {Service} opens TODAY at {time} (special hours)" checkout message (new i18n string × 38), shown only when the start comes from a special/extraordinary schedule. Both moved to fully-addressed + follow-up posted; awaiting Fabrizio's Confirm. **✅ SHIPPED THIS SESSION (2026-07-02) + moved to IN_TESTING:** house-number-after-street delivery address (cmqsn52d2, 407737cb — country-aware composeStreetLine, IT/EU=street-then-number, US/CA/FR=number-first); table-reservation screen polish (cmqwds5jt, 407737cb — localized date + clickable phone + email); save-a-card-without-active-service (cmr1u3qxm, 81ceec91 — Billing "Payment method" card via Stripe setup-mode Checkout, 38 locales); invoices show reseller company + VAT + number (cmr1ty0lc, ff86e228 — ResellerProfile.companyVatId pushed to BOTH branches + Imprint "Invoice details" field + invoice issuer VAT line). Coupon HIGH (cmqtllluu) — CORE verified-fixed (guest email-match, restaurant-sees-coupon on kitchen+admin order detail, max-uses, auto-email); accurate status posted.
  - **Promo "Get it Now" (cmqtmfp2n) FOLLOW-UP (Fabrizio 2026-07-02).** (a) ✅ FIXED (17d3d9d8): "20% of €102 shows −€1.20" — the promo has `oncePerOrder:true` (verified on prod config), which for %-promos means "discount only the single most expensive qualifying item"; the engine now EMITS the breakdown line for that path so the cart names the one discounted item (it returned [] before — that was also his "discounted items no longer visible" complaint for this promo), and the wizard checkbox got a %-specific hint ×38 explaining the ONE-item semantics. **Config decision for Fabrizio/Luigi:** if "20% ASPORTO" should discount ALL qualifying items, edit the promo and UNTICK "Only allowed once per order" (→ €20.40 on that cart). (b) 🔜 TO BUILD: quantity +/− steppers directly on the promo detail ("Get it Now") screen; also re-verify his original asks there (add-to-cart from the screen ✅ exists via "Start Adding Items"; items grouped by category — CHECK, he says they're mixed together).
  - **✅ SHIPPED (2026-07-02, 2a0633bb) — VIP specials + assigned offers removable per customer (Fabrizio).** Customer profile: via-GROUP specials get a remove button (removes THIS customer from that group — confirm spells out they lose all that group's specials; group untouched for others; API = membership-removal mode on vip-specials/individuals DELETE, same customerId-OR-email resolution as the listing); still-granted assigned offers get a revoke button (new DELETE /api/admin/customer-grants → status "revoked", audit kept; applied/redeemed protected server-side). 6 strings ×38. **Verify:** open fabrizio pisu's profile → trash the "15% DELIVERY via Vip" special → it disappears + his checkout no longer auto-applies it; trash the expired FP-08060B offer → card empties.
  - **✅ SHIPPED (2026-07-02, 184f8cae) — ONE public-promo rule + "why not public" transparency (Luigi's website-vs-ordering-page mismatch).** Luigi found the hosted site showing 4 Special Offers (incl. 2 VIP-only + 1 marketplace-only) while the ordering page showed 1 — hosted-site.ts specialOffers keyed on autoApply and skipped the VIP/channel/banner guards (VIP privacy leak). Now BOTH surfaces use the identical rule: active + Visible + Show-on-banner ON + no VIP links + website channel + window. Editor: VIP-linked promos get a "VIP only — attached to {groups}" notice INSTEAD of the Visible/Hidden+banner switches (they could never make it public). Promotions list: active-but-not-public promos now carry a reason badge ("VIP only — not shown publicly" / "Marketplace only" / "Banner card off") with explainer tooltips. 8 strings ×38.
  - **✅ SHIPPED (2026-07-02, 771a07bf; supersedes 8dcd015e/7f0ff06f) — kitchen tile lead = ONE line, GloriaFood-exact (cmqsn52d2, Luigi's reference photo).** Delivery/catering tiles: FULL address on a single uniform-size (text-base bold) line — street №, ZIP, city, comma-separated ("506 Collis Court, L9T5M7, Milton"), ellipsis-truncated. Show-name setting ON → name joins the SAME line in front (two-line name+address layout removed). Pickup/dine-in: name only, same size. Dedupe guard for legacy flat addresses embedding zip/city. Display-only — ring/print/poll untouched; **needs on-device look (Luigi + Fabrizio)**.
  **🔧 STILL OPEN (remaining reseller-report work):**
  - **Delivery time-slot ranges (cmqqxerxs) — QUEUED per Luigi 2026-07-02 (focused build).** GloriaFood-style: per-service backend option to offer time-RANGE slots (e.g. 20:15–20:30) instead of exact times. Customer picks a range; the kitchen countdown anchors to the range START; the restaurant can EXTEND and the customer email + app countdown extend too. Restaurant chooses granularity (15/30/45/60m) + range-vs-exact. Multi-part: schema (per-service slotMode + granularity; store the chosen window on the Order — reuse scheduledFor as the start + a new slotEndsAt/window minutes) + checkout picker (CheckoutModal slot enum builds ranges) + order storage + kitchen countdown (KitchenDisplay dueTs uses window start; "ready in" honors range) + the extend flow (order PATCH bumps window end → re-notify) + emails (confirmation shows the window). i18n × 38. Hot paths (checkout + kitchen poll) — build + verify carefully.
  - **Coupon (cmqtllluu) enhancements — the give-a-personal-offer screen.** Backend already supports per-SERVICE (orderType) + combinable (stackingRule) on assign-to-customer; expose them on the current personal-offer UX (Program 1 moved assignment to "Give a VIP special" / promo editor). Plus: make the checkout coupon box wait for the "Apply" button instead of applying on paste. Lower priority — the money-critical core is fixed.
  - **Invoices (cmr1ty0lc) — Fee Free's OWN issuer entity + VAT for DIRECT (non-reseller) restaurants.** Luigi chose "reseller for their stores, Fee Free otherwise." Reseller half shipped; the Fee Free half needs (a) Luigi to provide Fee Free's legal entity NAME + VAT NUMBER, (b) PlatformSettings.companyLegalName + companyVatId (both branches) + a superadmin input, (c) the invoice biller block to use them for the non-reseller case (falls back to "Fee Free Ordering" today). **Ask Luigi for the legal name + VAT, then build.**
  - **PayPal for monthly subscription billing (cmr1u3qxm) — DEFERRED per Luigi 2026-07-02.** Card-save shipped. PayPal-for-monthly needs a PayPal merchant / billing-agreement integration (capture PayPal auth, create a PayPal billing plan/subscription, handle PayPal webhooks, store paypalSubscriptionId). Separate larger project when Luigi wants it + has the PayPal merchant account.

- [x] **✅ SHIPPED (2026-07-02) — Exclude gift cards from promo/coupon discounts (Luigi 2026-07-01, money-leak).** New `promoExcluded` flag on MenuItem + MenuCategory (pushed to BOTH Neon branches; category ORs with item). Engine: excluded lines never match any item group (one choke point covers BOGO/buy-N/free-item/free-dish/combos/bundles/grouped-%), whole-cart bases (%-off, fixed_cart, payment_reward, fixed_combo) use the new `discountableSubtotal()`, minimumOrder + free-item triggers judged on it too, and the summed discount is capped at it in BOTH apply-promos (preview) and orders (charge). ALSO excluded from the reward-redeemable total: `reserveCredit` base = total − excluded lines at charge, preview returns `redeemExcludedTotal` and CheckoutModal caps the slider the same way — credit can't buy store credit. Owner UI: "No discounts on these items" editor on /admin/promotions (mirrors the Reward earn-exclusions editor; PATCHes promoExcluded; 9 strings ×38 locales, parity 0 mismatches). 9 engine tests (fixed_cart/%-off/grouped/minimumOrder/free_item/bogo/payment_reward/legacy). **Luigi to do:** open /admin/promotions → "No discounts on these items" → toggle your Gift Cards category to "No discounts". Original note: ~~a $10 coupon applied to a $10 Gift Card item → gift card bought for $0.~~

- [~] **Delete the two mistaken "Test July" restaurants under Fabrizio's reseller (Luigi 2026-07-01).** DELETE BUTTON BUILT + DEPLOYED (56bf86e6): Super Admin → open a restaurant → red "Delete" button → type the restaurant name to confirm → permanently removes it + all its data (retry-until-stable cascade, dev-verified, fail-safe). **Luigi TO DO:** after the deploy is live, go to superadmin/resellers/cmpxadebw000i04jm90sddtei, open each **"Test July"** (free), and click Delete. **Leave alone:** "Kaori Japanese Restaurant | Varedo" + "Japanese Restaurant | TEST".

- [~] **Order page — category headers as HERO banners with the name overlaid (Luigi 2026-06-30). BUILT 2026-07-01 — pending on-device confirm.** Shipped as `CategoryBanner` in `src/app/order/[slug]/OrderingPageClient.tsx` across all 3 layouts (grid/list/carousel): photo banner (156px desktop / 130px mobile, 12px radius) with bottom scrim + gold accent bar + white shadowed name; themed `theme.primaryColor` fallback for image-less categories (with an `isLightColor` luminance guard that flips the text dark on a pale brand colour); chevron collapse toggle (whole band is the tap target) or the carousel scroll arrows when not collapsible; gated on `theme.showCategoryImages` (owners who turned category images OFF keep the plain text header). Verified locally (desktop + mobile, collapse toggle, no console errors) + preflight green. **Follow-ups:** (1) the count pill from the mockup ("N dishes") was deferred — it's the one new user-facing string and `CategorySection` has no `t()`; add it with an ICU-plural key across all 38 locales when convenient. (2) On-device visual confirm on a real store (Luigi's brand-red primary → red fallback bands). Original spec below for reference:
  Luigi reviewed mockups and picked **Option A — "banner band"** (compact hero, NOT the tall hero). Build spec:
  - Each category header = a full-width banner using the category's `imageUrl`. Height ~150px desktop / ~130px mobile, consistent aspect, `border-radius: 12px`.
  - **Bottom-up dark scrim** (`linear-gradient(to top, rgba(0,0,0,.72) 0%, rgba(0,0,0,.14) 50%, transparent 74%)`) so the name is legible on any photo.
  - Overlaid text bottom-left: a **small accent bar** (~34×3px, rounded) above the name — gold `#f0c674` on photos, white 75% on the themed fallback; then the category **NAME** (~24–25px, weight 500, letter-spacing -0.01em, white, `text-shadow: 0 1px 12px rgba(0,0,0,.5)`); then a **count pill** ("N dishes", translucent white `rgba(255,255,255,.18)` bg, rounded).
  - **Chevron** collapse toggle overlaid top-right in a translucent dark circle; the WHOLE banner is the tap target.
  - **Fallback (no imageUrl):** solid block in `theme.primaryColor` (NEVER hardcode orange — theme-colors rule) with the same accent-bar + name treatment.
  - Use `next/image` with correct `sizes` + lazy loading — hot customer path, don't ship unbounded full-res images. Keep the top category chip-nav as-is.
  - i18n: the "{count} dishes" label needs a key + all 38 locales (ICU plural). Optional polish: gentle darken/zoom on tap/hover (desktop).
  Deferred behind the B5 promo-usage-ledger work in progress; this is the next build after B5 commits.

- [ ] **Promotion usage ledger — usedCount reconciliation job (small follow-up, 2026-06-30).** The `PromotionUsage` per-order ledger is now BUILT (schema + `src/lib/promo-usage.ts` + claim/release wiring), and it eliminated the two edges adversarial review found in the raw-counter model — (A) concurrent double-decrement and (B) mutable-cap desync — because release is now `DELETE ... RETURNING` off the ledger row (idempotent, cap-independent). One bounded, SAFE-DIRECTION gap remains: the atomic `usedCount` bump (before `order.create`) and the ledger-row write (after) are two statements, so a process death / DB blip between them can orphan `usedCount` **high by 1 with no ledger row** — over-counts the cap → under-redeems (NEVER over-redeems / cap-busts), and there's no row for release to delete so it doesn't self-heal. Same risk CLASS the coupon counter already accepts. Two ways to close it (either is fine): (1) wrap `order.create` + the `promotionUsage.createMany` in one `prisma.$transaction` so they commit atomically (keep the counter claim OUTSIDE the txn so the Promotion row lock isn't held during create — avoids cap-stampede contention); OR (2) a periodic reconciliation that sets `usedCount = COUNT(PromotionUsage rows)` per promo (also self-heals any historical drift). Low priority — rare + safe-direction.

- [ ] **Phone uniqueness — normalized column for format-proof matching (Luigi 2026-06-27).** Signup now blocks a new ACCOUNT reusing a phone already on another account (exact match on the entered string; `(restaurantId, phone)` indexed). It does NOT catch format variants ("(905) 385-4444" vs "9053854444"). To make it format-proof: add a `Customer.phoneNormalized` column (digits via `normalizePhone`), backfill, index it, and match on that at signup (+ optionally for guest dedup). Same idea could harden email (already lowercased). Low priority — exact match covers the common case.

- [ ] **From the VIP/promo polish audit (2026-06-27) — remaining (verified, not yet done).** Safe fixes already shipped (49c76ffe: dead-key purge, inactive-promo notify guard, dedup scoping, account discount-label i18n + helpOffers tip, chip currency, add-members guard, attach HelpTip). STILL OPEN: (a) **CheckoutModal pre-existing hardcoded English** [i18n, predates VIP work]: guest sign-in CTA, PayPal "not connected"/redirect notices, coupon box "Apply"/"Code … applied"/"…", tip "Suggested" badge — add keys + translate 38. (b) **VIP UX polish:** confirm() before remove-member / detach-special / remove-target (detach changes checkout — warrants a guard) [i18n]; replace bare "…" busy text with a translated label + the prompt()/confirm() rename/delete with inline edit; initial-fetch loading state on the VIP Specials page (sections briefly flash "create one first"). (c) **"Your coupons" heading** now lists auto-apply VIP specials → consider renaming to "Your offers" (Luigi's terminology call). (d) **Per-item "You saved" dedup** shows the menuItemId total on the FIRST matching line only — if the same dish is on multiple cart lines it over-attributes to one line; needs per-LINE breakdown from the engine to split correctly. (e) **Individuals API:** GET omits phone-only targets (not listed/removable); phone matching in vip-membership is exact-string (no normalization) — Phase-1 VIP is email/account-driven so low impact, but normalize symmetrically if phone targets become real.

- [x] **DONE (d93b6f5b) — Cart: per-item "You saved" badges, GloriaFood-style (Luigi 2026-06-27, Fabrizio).** Inline green "👍 You saved X" badge on each discounted cart line in CheckoutModal; bottom summary simplified to one line per promo; whole-cart promos stay at the bottom. Display-only. (Cart DRAWER still shows bottom-only — extend there too if Luigi wants.) ~~We already compute the per-dish breakdown (`promoBreakdown` → `p.breakdown[]` with menuItemId+amount) and render it as a SUMMARY list at the bottom of the cart (CheckoutModal ~611-630). Luigi/Fabrizio want it ATTACHED inline to each discounted item row instead — a green "✓ You saved: X" badge under the line item (+ the discounted line price), exactly like GloriaFood — and ONLY a bottom line when the promo is whole-cart (no item targeting). Plan: build a `Map<menuItemId, savedAmount>` from all applied promos' breakdowns; in the cart ITEMS list (both the cart drawer + CheckoutModal item rows) show the badge on matching lines; keep whole-cart promos (no breakdown) at the bottom as today. Gotcha: breakdown aggregates by menuItemId, so dedupe when the same menuItemId appears on multiple lines (show once / split). New i18n: "You saved {amount}" across 38. Display-only — never touches the charge.~~

- [ ] **VIP Groups — PHASE 2: recurring auto-send scheduler (Luigi 2026-06-27).** Phase 1 (member-only auto-apply specials = attach an existing promotion to a group) SHIPPED (dce977a3/8f1a3f1e). Next: let a group↔promotion link run on a schedule — **monthly / weekly / daily / one-time** (Luigi picked all). Add per-link schedule fields to `CustomerGroupPromotion` (cadence, anchor day, next-run, active) + a cron that re-applies/re-grants on cadence. Primary use case: monthly perk for a team. Builds toward Phase 3.
- [ ] **VIP Groups — PHASE 3: "LuigiBucks" store credit + account-credit promo (Luigi 2026-06-27).** Build Program 2 (per-customer store-credit wallet, see plan P2.0–P2.5) + a NEW promo type that **adds $X to a customer's account, no purchase necessary**. Then a VIP group can apply that credit promo **once or recurring** (Phase 2 scheduler) → e.g. monthly team credit. Highest-risk (payment path) — do Program 2 wallet first, redeem-as-payment carefully.
- [ ] **VIP Groups — cleanup: remove the deprecated inline assign-promotion route/UI keys.** Phase 1 removed the inline "Send a deal" UI but kept `POST /api/admin/customer-groups/[id]/assign-promotion` + its `admin.customerGroups.assign*` i18n keys (unused). Remove the route + dead keys once Phase 1 is verified, OR repurpose the route for Phase 2 recurring grants.

- [ ] **Promotions — per-type DISPLAY/UX polish (deferred from per-type debug, 2026-06-27).** Correctness/engine bugs all fixed + tested; these are display/clarity items. ✅ DONE: (1) partial-% badge now shows the real "% off" for bogo/buy_n_get_free/free_dish_meal in the guided modal instead of "FREE"; (2) free_dish_meal/free_item per-dish breakdown; (4) payment_reward redundant Step-3 payment control hidden. STILL OPEN: (3) meal_bundle_speciality "What you get" omits the per-item upcharge — own copy [i18n]. (5) payment_reward optimistic preview vs defaulted "cash" — frame conditionally. (6) percentage_off "specific items" multi-group editor → single combined selector (ties into nudge rework). (7) breakdown lines not summing to headline for overlapping groups (charge correct; display reconciliation). (8) buy_n_get_free detail modal doesn't mark which group is FREE. (9) free_delivery on a non-delivery channel = inert config trap (warn in wizard) [i18n]. (10) save-time guards: bogo/buyN fixed_percent at 0%, and combos/bundles with untargeted groups [i18n]. (11) **meal_bundle FULL eligibility at order time** (min order / customer type / usage cap / time-of-day) — only start/end window + isActive enforced for bundles today (they bypass the engine).

- [ ] **Code-unlocked "secret" bundles (Luigi 2026-06-27).** Today a Meal Bundle must be Visible (built from a "Build your deal" card); a coupon code can't open the composer, so Hidden + code is inert (we now hide those controls for bundle types). If Luigi wants a code-gated bundle (e.g. enter SANDWICH3 → reveal a "3 sandwiches for $30" composer), build it: entering the code surfaces/opens the bundle composer for an otherwise-hidden bundle. Until then bundles are visible-only. Also: the bundle highlight should use the item-based trigger from the nudge-rework note (eligible item added → highlight).

- [ ] **Marketing website "Special Offers" tiles redesign (Luigi 2026-06-27).** The special-offer tiles on the hosted marketing site (`src/app/site/[slug]/page.tsx` ~718-760) look plain/bad — replace them with the same styled promo tiles used on the ordering page (image card + headline + CTA), or a close match that suits the website. UI/design task.

- [ ] **Deal modal: true step-by-step walkthrough (Luigi 2026-06-27).** The BOGO / multi-item "Build your deal" modal should guide the customer ONE item at a time — "Select item 1" → pick → "Select item 2" → pick → "Deal applied" — like GloriaFood (screenshots), instead of showing all groups on one scrollable page. Applies to BOGO / buy-N-get-free / combo / bundle pickers. UX enhancement.

- [ ] **Promotions — nudge/highlight rework (Luigi 2026-06-27).** Three linked asks: (1) **Match GloriaFood's exact Standard/Exclusive/Master wording/ideology** in our stacking explainer + tooltips. ⚠️ Decide a real semantic difference: GloriaFood "Exclusive = no other promos on the same **ITEM**"; ours is per-**ORDER** (an exclusive blocks all other standards on the whole order). Either reword to match our per-order behavior clearly, or change the engine to per-item exclusivity (bigger). (2) **Hide the nudge/"Start nudging at" + highlight option entirely when a promo is Hidden** (coupon-only) — a hidden promo should have no highlight/nudge controls. (3) **Item-based nudge:** for promos defined by ITEMS not a $ threshold (BOGO, combos, bundles, free_item), the highlight should fire when an **eligible item is added to the cart**, and let the owner **choose which item group triggers it** (GloriaFood "Highlight promo → Custom selection → item group 1 / item group 2", screenshot). Current nudge is $-threshold-only (highlightThreshold) — this is the deferred audit dead#10 done properly.

- [x] **🎉 PROGRAM 1 — Promotions overhaul SHIPPED 2026-06-26 (merged d8a13479, deployed).** Coupons fully retired → promotions (UI removed, "Promotions & Coupons"→"Promotions", migration ran on prod: 2 personal coupons → 2 hidden promos + grants, 1 popup→visible, 8 hidden rows coerced). Visible/Hidden model replaces the 3-mode Display Mode; dead "popup" mode + Limited Showtime removed. Assign-a-promotion-to-a-customer (by email, account or not) + always-send email. **F-92452C bug fixed + verified on prod** (code+email match → applies, wrong email → "registered to a different email"). Cart shows which dishes a % promo discounted; promo category picker groups by menu (multi-menu). 21 engine tests, 38-locale parity. Plan: `~/.claude/plans/save-thse-tests-for-calm-jellyfish.md`.
  - **Fast-follow i18n polish (low priority):** admin.assignCoupon / customerDetailPage / customer.accountPage.yourCoupons copy still says "coupon" in places — reword to "offer/promotion" across 38.
  - **Verify on prod once deployed:** see the Program-1 test checklist (assign→email→redeem by email match; wrong-email rejection; Visible/Hidden editor; cart which-dishes; multi-menu picker).

- [ ] **PROGRAM 2 — Store credit (wallet) + loyalty points (PLANNED, not started).** Per-restaurant opt-in wallet ("LuigiBucks"/"$ Credit") usable as a payment method + loyalty points → credit; funding = manual grant / promo-reward / refund-to-credit / signup bonus (+ top-up deferred). Full phased design (P2.0–P2.5) in the plan file. Mirrors the coupon-ledger pattern (append-only ledger + atomic balance). Build after Program 1 is verified.

- [x] **🐛 FIXED (2026-06-26, pending deploy): clicking an order in Reports → List View 404'd.** The List View linked each order to `/admin/orders/{id}`, but no such route existed (only the live queue at `/admin/orders`, which expands rows in place and isn't deep-linkable). Built a read-only admin order-detail page at `src/app/admin/orders/[id]/page.tsx` — customer, items+modifiers, full price breakdown (subtotal/discount/delivery/tax/tip/total), payment, scheduled time, notes, status badge. Ownership = order.restaurantId ∈ `resolveReportScope().ids` (single store → itself; brand parent → any location), matching how the report lists them + secure against cross-tenant reads. Reuses existing i18n keys (no new strings). Affects ALL restaurants. Preflight green; route registered.

- [x] **Reports export — labeled top-right button on every report (DONE 2026-06-26, pending deploy).** The export was a tiny unlabeled download icon hidden at the bottom-right of each sub-report's table card (Luigi couldn't find it). Moved to a labeled "Export" button in the header (top-right, next to the date picker) across all 16 reports — Sales Trend/Summary, List orders/clients, Menu Insights items/categories, Promotions (relocated from bottom), + Visits/Reservations/Funnel/Clients/Google-Rank/Connectivity/Heatmap/EOD (labeled in place). Each still forwards `buildQuery(sp)` (full range + view dim + location/scope + status/search filters) so PDF/XLS/CSV downloads honor exactly what's selected. Preflight green.

- [x] **Promo Popup report (cmqp8z9ko) test polish — DONE 2026-06-26 (pending deploy).** A (popup→promo) + C (popup→URL) confirmed working by Luigi. Two changes shipped: (1) the PromoDetailModal footer is now ONE full-width, centered CTA reading "Start Adding Items" for every build-your-cart promo (was a small bottom-right "Got it"; `fixed_cart` like the "$10 Coupon" now reads correctly) — free_delivery keeps its "switch to delivery". (2) Removed the **coupon** option from the Promo Popup button (admin) — only URL or Promotion now, since coupons are being folded into promotions (one step of the [[coupon↔promo unify]]). Reused existing i18n keys (startAddingItems/switchToDelivery), no new strings. Preflight green.

- [x] **Closing-days fixes (cmqp8l948) — DONE 2026-06-26 (pending deploy + on-device).** Three issues + Luigi's rule "exceptional hours must stay within the service's normal hours; can't close a service when it isn't open":
  1. **Cross-midnight closures no longer silently dropped** — `parseHolidayRules` now keeps `open !== close` intervals (was `open < close`); `hhmmInsideIntervals` handles the wrap. (`holiday-rules.ts`)
  2. **Within-service-hours validation** — new shared `validateHolidayRulesAgainstHours` + `holidayWindowOutsideService` (uses `pickHoursForService` + `rowIntervals`): an exceptional open/closed window must fit inside the governing service's hours for that day (per-service → that service; dine_in/take_out/catering/all → general). Wired into the save API (authoritative 400 `window_outside_service_hours`) AND the admin form (clear rejection toast). A pickup 10pm–2am closure (pickup is 9am–11pm) is now REJECTED with a clear message; cross-midnight only allowed when the service's own hours cross midnight (e.g. general 10am–3am).
  3. **Start date required** — `addHoliday` now shows "Pick a start date first." instead of silently no-op'ing.
  4. **/info shows special closures** — info page now loads today+future holidays + renders an amber callout (full closure / closed windows / custom hours) + the owner's custom message, reusing the `ordering` namespace closure keys.
  Unit tests added (holiday-rules.test.ts, 18 pass; 50 hours-related total). New i18n keys `admin.hours.holidayNeedsDate` + `holidayWindowOutsideService` across all 38 locales (parity verified). Preflight green.

- [x] **🔔 FIXED (2026-06-26, pushed — pending on-device confirm): a LOGGED-OUT kitchen device kept ringing.** Luigi: opened the S23, saw "logged in on another device" (he'd logged the kitchen in on his computer earlier), yet the S23 had just rung for a new order. ROOT: push/ring lifecycle wasn't tied to the single-active-session lifecycle. The native screen-off ring is driven by `/api/kitchen/alarm-state?token=…` (TOKEN-auth, no session) → it rings as long as the device's `KitchenPushToken` row exists. Logging in on the computer (a desktop browser, which registers NO FCM token) superseded the S23's *session* (`Restaurant.kitchenSessionToken`) but left its push token → it stayed the sole ring target. Also the ~4s `/api/kitchen/orders` ring poll never checked session freshness, so a foreground stale device kept ringing via web audio until its heartbeat bounced it. FIX (3 parts, all shipped): (1) `auth-kitchen.ts` — on every kitchen login, `deleteMany` all `KitchenPushToken` for the restaurant (the new active device re-registers on launch); decisive fix — `alarm-state` then returns `ringing:false` for the old device. (2) `api/kitchen/orders` GET — enforce `checkKitchenSessionFresh()` → `session_superseded` 401 (cheap: short-circuits with no DB read for admin-session viewers). (3) `api/kitchen/register-device` POST — reject when stale so a superseded device can't reclaim ring ownership. Preflight green. ⚠️ ON-DEVICE TEST still needed: log kitchen in on device A, then log in on device B → place an order → A must NOT ring (screen-locked) and must show the superseded screen on open.

- [x] **💰 FIXED (2026-06-26, local — pending deploy + on-device confirm): once-per-lifetime promo: preview ≠ charge AND returning customers wrongly blocked.** Found while testing report #1 (order ORD-697157388 / cmqufxkyf…): "Sameem" saw checkout **$10.53** ("$10 Coupon" `fixed_cart`, `onceLifetimePerClient`, −$10) but was charged **$21.83** (`promoDiscount: 0`). TWO root causes: (1) `apply-promos/route.ts` never computed `hasUsedLifetime` → preview always optimistic; (2) `orders/route.ts` used a COARSE heuristic ("any prior promo-discounted order ⇒ block ALL lifetime promos") that over-blocked returning customers from brand-new lifetime promos. FIX (Luigi chose per-promo-via-history): new shared `usedLifetimePromoIds()` in `coupon-ledger.ts` = precise ledger (email/phone) + a per-customer `appliedPromos` order-history scan (covers pre-ledger redemptions, scoped to one customer so it scales). BOTH the order route and the preview now call it → preview == charge; a genuine first use applies, a real second use is blocked. Coarse heuristic removed. Verified on prod data: Sameem (77 orders/12 promo'd, never used THIS promo) → correctly allowed; build green (preflight exit 0).

- [ ] **⚠️ Test EVERY promotion type 1-by-1 WITH Luigi (some likely broken / not set up to work).** Luigi
  (2026-06-25): we tested a few promo types together but NOT all — especially the more complex ones — and he's
  confident several aren't working correctly, possibly not even configured to work. Go through every promotion
  type together, test each end-to-end, fix what's broken. Do NOT mark promo behaviour verified without this
  JOINT pass — it needs Luigi. (The coupon usage-LIMIT bug is separately fixed + shipped: 9c504b23 / e9a22828.)

- [ ] **Promotion editor: label/group the categories by menu (multi-menu stores).** When choosing which
  categories a promotion applies to, categories from ALL menus are merged into one flat list with no
  indication of which menu each belongs to — impossible to tell apart for a multi-menu restaurant. Group or
  label them by menu in the picker. _(Fabrizio, 2026-06-25, on the Coupon report.)_

- [ ] **Cart: summarise which items a promotion discounted (GloriaFood-style).** When a promo discounts only
  certain items (not the whole order), show a small summary in the cart of which products received the
  discount, so the customer can see what's covered. _(Fabrizio, 2026-06-25, on the Coupon report.)_

- [ ] **Move the Kitchen Alert Sound setting to the Order Handling page.** The custom kitchen-alert-sound
  uploader (`kitchenAlertSoundUrl`, saved via `/api/restaurants/kitchen-sound`, currently on the Restaurant
  Profile page) should live on `/admin/order-handling` (Taking Orders), alongside auto-accept + scheduled
  orders. ⚠️ Honor the relocation GUARD — order-handling saves ONLY via `PATCH /api/admin/order-handling`;
  don't fold the sound into another page's save payload. Worth a HelpTip noting the custom sound applies to the
  BROWSER kitchen view — the native app intentionally plays the built-in alarm (suppresses custom to avoid the
  two-engine overlap Fabrizio hit). _(Luigi note, 2026-06-25.)_

- [x] **Google Ranking report — the "FIX" actions are now actionable.** DONE 2026-06-25: each `SeoCheck`
  in `src/lib/seo/health-check.ts` now carries a `fixHref` → Content optimization → `/admin/profile`,
  Google Business + Social → `/admin/social-media`, Domain + Security → `/admin/publishing`, Structured data →
  `/admin/marketplace`; the report renders a translated "Fix this →" link on every failing factor (parity 4885).
  Misleading "Profile → Social Links" hints corrected to "the Social Media page".
  STILL PENDING: (a) PageSpeed Insights returns 429 (keyless API rate-limit) → cache the result per restaurant
  (~6h) so it doesn't re-probe + show "UNKNOWN" every load; (b) the `SeoCheck.hint` strings are still
  hardcoded English in `health-check.ts` (rendered English in all 38 locales) — move them to i18n keys
  (hint→hintKey+params) while keeping an English `hint` fallback for the English CSV/PDF export.

- [ ] **Kitchen app login banner overlaps the phone status bar (Android + iOS).** On the native kitchen app
  login screen the top floating switcher (`AuthLanguageSwitcher`, `absolute top-4 right-4`) sits in the unsafe
  area and overlaps wifi/battery. Root: `src/app/login/layout.tsx` has NO `viewport` export (so no
  `viewportFit:"cover"` like the kitchen pages do), and the switcher's `top-4` ignores `env(safe-area-inset-top)`.
  FIX (web-only, no native rebuild): add `export const viewport = { …, viewportFit:"cover" }` to
  `login/layout.tsx` + `paddingTop: max(.., env(safe-area-inset-top))` on `AuthLanguageSwitcher`. Reuse the
  kitchen pattern (`KitchenDisplay.tsx:3190`). _(Luigi note, 2026-06-25.)_

- [ ] **Windows-PC kitchen display not ringing reliably ("for later").** Desktop browsers lock audio under the
  autoplay policy until a user gesture; only iOS shows a "tap to enable sound" gate (`KitchenDisplay.tsx`
  `soundGateOpen` ~1121-1128). FIX dir: show the enable-sound gate on desktop browsers too + a persistent
  "sound is off" banner while pending orders exist. _(Luigi note, 2026-06-25.)_

- [ ] **PrintNode → opt-in admin setting that appears in the kitchen display ("for later").** Mostly built:
  `Restaurant.printNodeEnabled` + the admin toggle (`KitchenWorkflowToggle.tsx`) + KDS hydration + print-fallback
  all exist. GAP: the `PrinterSetupModal` shows the PrintNode tab regardless of the toggle — gate it on
  `printNodeEnabled` so PrintNode only shows when the owner enabled it (less-common path; most use native WiFi
  printing). _(Luigi note, 2026-06-25.)_

- [ ] **iOS Kitchen Order App — test completely + move toward launch.** On TestFlight via Codemagic; pending
  native StarXpand thermal-print bridge + ring-when-locked, then full device test + App Store submission. See
  [[project_ios_app_state]]. _(Luigi note, 2026-06-25.)_

- [x] **Reseller "Login page" explainer — SHIPPED (cea12162, 2026-06-25).** Free partners now get an explainer
  (neutral `restaurantownerlogin.com` login + `feefreeordering.com/login` fallback + upgrade-to-Branded +
  Kitchen Order App badges) instead of an empty redirect. ⚠️ App badges are "coming soon" (non-linking) until
  the apps hit the public stores — wire the real store links at App-store launch (#33). _(Luigi note, 2026-06-24.)_

- [ ] **Reseller section + multi-restaurant (brand-parent) section — full UX reboot.** When there's time, do a
  complete usability reboot of BOTH the reseller admin section AND the multi-location / multi-restaurant
  sections to improve the experience. _(Luigi note, 2026-06-24.)_

- [ ] **Split hours — DONE (reservations shipped 2026-06-24, commit 84662c6a).** v1 shipped general + per-service split hours; since then the
  deferreds A/B/C also shipped (2026-06-24): (a) server-side weekly-hours enforcement for scheduled orders
  (orders/route.ts fail-open backstop), (b) reserve-then-order holiday gate, (c) menu-schedule coverage gaps use
  real intervals. **Remaining = Reservations split hours** — reservations still resolve a single window via
  `ReservationSettings.reservationHours`; the reservation `OpeningHours` rows ALREADY store intervals from the
  admin editor, so the work is the READ path: `resolveDayHours` / `validateBooking` (`reservation-validation.ts`)
  + the `ReservationModal` slot-gen need to use `rowIntervals` instead of a single open/close, and decide the
  precedence vs the legacy `reservationHours` field. _(Owner scoped reservations out of v1; pending a go-ahead.)_
  Minor nicety: the CheckoutModal **exact-time** picker bounds to the day's envelope, so a customer could TYPE a
  gap time — the server now rejects it (A) and the slot DROPDOWN already skips gaps; only client pre-validation
  is missing.

- [ ] **Ordering page — collapsible menu categories on desktop.** On the customer ordering page
  (`/order/[slug]`), on **desktop**, give customers the option to expand/collapse the menu categories
  (collapsible category sections), so a long menu isn't one giant scroll. _(Luigi note, 2026-06-24.)_

- [ ] **VIP member groups.** Owners assign specific customers into VIP groups that unlock features/rewards
  per group (auto-rewards, special promos, etc.; multiple groups supported). The customer-accounts system +
  the Coupon / Promotion / CustomerCoupon engine already provide the building blocks — this adds the
  tier/group concept, the admin UI to manage groups + membership, and the per-group feature hooks.
  _(Luigi vision, 2026-06-24; surfaced while wiring saved delivery addresses.)_

- [ ] **LOW / someday — Kitchen tile: ONE setting to append extra delivery info (Luigi 2026-07-04, deliberately parked at the bottom).**
  If we ever build it: a SINGLE on/off setting ("show extra delivery info on the order tile") — NOT a
  per-field picker. When on, append the extra info (apartment/unit, buzzer/intercom, floor, phone —
  whatever the order carries in `deliveryAddressData`/`customerPhone`) after the existing tile text,
  separated by " - ". **Never include zip/postal (not necessary).** The device-confirmed tile layout
  (name/address lead, two-line format) stays the default untouched; extra info only renders when the
  setting is on. Implementation notes from the earlier scoping: render in `KitchenDisplay.tsx` OrderRow
  (~L469-549), field data already returned by `/api/kitchen/orders`, save via `/api/restaurants/profile`,
  i18n ×38, display-only (ring/print/poll untouched) → needs an on-device confirm.

## Done

<!-- Move items here when shipped, e.g.:
- [x] Short example — fixed in `abc1234` (2026-06-24).
-->

## Promo popup — relocate to Marketing + link to a promo/coupon (Luigi, 2026-06-25)
- MOVE the Promo Popup OUT of Admin → Profile into its OWN heading under the MARKETING section
  (not under Restaurant Basics / Profile).
- BUILD OUT: the popup must be able to link DIRECTLY to a promotion or a special coupon from the
  promo station (select/deep-link a promo or coupon), not just a free-form URL.
- THEN update Fabrizio's reseller comment on the "Popup" report (cmqp8z9ko000304kykoin8wuw):
  DELETE the old "Profile -> Promo popup" comment + post a corrected one reflecting the new
  Marketing location + the promo/coupon linking.

## Saved-address form should match the checkout delivery UX + country config (Luigi/Fabrizio, 2026-06-25)
- The customer ACCOUNT page "Saved delivery addresses" form (AddressBook, /order/[slug]/account)
  is a plain text form (Label / Street / City / Postal). It should work the SAME as the checkout
  delivery-area entry: address AUTOCOMPLETE (type -> choose the exact match) + confirm with a PIN
  ON A MAP, and reflect the restaurant's configured country/area address fields
  (resolveDeliveryAddressConfig) — not a generic Label/Street/City/Postal set.
- FIX: reuse the checkout's address-entry component (autocomplete + Leaflet/Google pin + per-country
  fields) inside the account AddressBook add/edit form so the two are identical.
- NOTE: Italy DOES use postal codes (CAP, 5 digits), so the postal field isn't wrong per se — the
  real issue is the form ignores the restaurant's address-field config + lacks autocomplete/map.
- This is feedback on the IN_TESTING "Saving multiple delivery addresses" report (cmqqt9zyl).

## Kitchen Order App — remove the "Admin login" button (Luigi, 2026-06-25)
- From the Kitchen Order App, clicking "ADMIN login" navigates the user OUT of the app (to the admin
  login). That shouldn't be possible from the kitchen display.
- REMOVE the "Admin login" button/link from the kitchen login screen / kitchen display. Web-only
  change (the app loads the remote /kitchen URL — no APK rebuild needed).

## Coupon codes vs promotions — redundancy / unify (Luigi, 2026-06-25)
- Luigi: the standalone "New coupon code" (Coupon model) largely DUPLICATES a "% discount on cart"
  or "Fixed discount on cart" promotion attached to a code. Consider REMOVING the separate
  "New coupon code" flow + letting ANY promotion optionally require a coupon code (unify under promos).
- NUANCE to preserve: the Coupon model ALSO powers PERSONALIZED coupons (assign a code to a specific
  customer + email it — the "Coupon created -> email customer" feature, report cmqa6lls1). Must be kept
  (fold into the promo flow or a lightweight personalized path) before retiring the standalone flow.
- Foundation exists: promotions already support Promotion.couponCode (apply-coupon "source: promotion").
- DECIDE + build in the PROMOTIONS 1-by-1 joint pass (reserved). Touches: promo wizard (a "require a
  code" option on Restrictions & Display), the apply-coupon flow, personalized coupons, and the admin
  Promotions/Coupons UI (remove "New coupon code" / "Coupon Codes" tab).

## Reward Dollars + VIP scheduler — deferred follow-ups (captured 2026-06-27)
Programs 2+3 BUILT this session (see memory project_reward_dollars_and_scheduler.md). Deferred:
- Refund-to-wallet on a FULL refund: return spent Reward Dollars + claw back earned credit
  (refund route currently only caps the card refund at the charged amount = total − creditApplied).
- Phone-normalized match column for strict identity matching (VIP + reward), so phone reuse can be
  enforced/queried without per-row normalization. (Email/sign-in matching already strict.)
- ScheduleEditor on the VIP-home individuals (raw-email) section — currently surfaced on the group
  page + customer profile only.
- Reward expiry enforcement: rewardExpiryDays is STORED but not enforced (needs an expiry cron).
- Customer top-up / buy credit (Stripe charge → credit); chain-shared wallets — both deferred.

## Reward Dollars earning + dedup (captured 2026-06-27, session 2)
SHIPPED this session: configurable earn rules (signup-window / first-order / order-over / nth-order) on /admin/rewards, "Grant Reward Dollars" promo type, pay-with-credit auto-on when the program is enabled (shown on Accepted Methods page), and the "Fix duplicates" button now MERGES duplicate categories. Deferred follow-ups:
- Item-level dedup WITHIN a merged category currently deletes only exact-duplicate items during a category merge; a standalone "merge duplicate items in this category" pass could be added (the live FK is SetNull, verified — item deletion is safe).
- Birthday + referral earn triggers (need a Customer.birthday column + a referral link/flow).
- Optional "earn-only mode" (let a store earn but NOT allow spending). Current design = opt-in means customers CAN pay with credit (per Luigi). Add a toggle only if Luigi wants earn-only.
- Show the earned-credit amount on the order confirmation/receipt + a reward_credit promo's amount in the customer's "My offers".

## Pizza half/half + copy-settings (captured 2026-06-27, session 3)
SHIPPED: half/half pizza pricing now charges HALF for a topping/sauce/cheese on one half (client computePrice + server (L.H)/(R.H) halving, unit-tested); "Copy settings to other items" (item row → modal → pick sections + target items/categories → /api/menu/items/[id]/copy-settings, clones modifiers + remaps pizzaConfig). Deferred:
- Copy-settings: variant-SCOPED modifier groups aren't copied (cloned as item-level); combo config not copyable (item-specific). Fine for v1.
- Pizza extra-quantity bump: server doesn't add the extraQuantityMultiplier bump at all (pre-existing; only matters when a restaurant sets it >0, default 0). Revisit if a store uses it.

## Reward Dollars — auto-reject did NOT return spent credit (FIXED 2026-06-29, found via Luigi's prod test)
BUG (money): when an order paid partly/fully with Reward Dollars sat un-accepted and the auto-reject safety-net cron fired (4 min, status→"rejected"), the spent credit was NOT returned — only the MANUAL reject path (PATCH /api/orders/[id]) called releaseRewardForOrder. Coupons, promo-usage and the card auth were released; reward credit was stranded in status="applied" forever. Same gap in the abandoned-payment → "cancelled" sweep.
FIX: added `releaseForOrder` (reward) to both paths in src/lib/auto-reject-orders.ts (idempotent, no-ops when no credit spent). Now full parity with manual reject. Verify on prod: place a credit order, let it time out (or trigger /api/cron/auto-reject-stale-orders), confirm balance returns.

## Reward Dollars — per-order detail in customer wallet activity + receipts (flagged 2026-06-29, Luigi)
Luigi: the customer account "Pizza Bucks" activity list shows only generic "Spent on an order / Earned on an order / Returned" + date + amount — needs to show, PER ORDER: the order number, payment method, the order breakdown (subtotal/discounts/tax/tip/total), and the reward bucks USED + EARNED on that order.
- Account activity (src/app/order/[slug]/account/page.tsx ~line 234): ledger query currently selects only id/amount/reason/createdAt. Add orderId(+note); for order-linked rows show order # and link to its receipt; show paymentMethod. EARN test on prod CONFIRMED working (+$4.50 = 5% of $90, order dmftdh97).
- Order receipt/confirmation/status (src/app/order/[slug]/{confirmation,status/[orderId]}/page.tsx): add "Paid with {rewardName}: −$X" + "Earned: +$Y" lines to the existing breakdown + show payment method.
- i18n: new strings across all 38 locales. Add HelpTip where non-obvious.

## Reward Dollars — print used/earned on the PHYSICAL receipt (Luigi 2026-06-29)
On-screen receipt/confirmation/account now show reward used/earned (commit 04be4c14). Luigi: the THERMAL receipt must show them too. Payment method ALREADY prints (payment section). Adding "Paid with {rewardName} -$X" + "You earned {rewardName} +$Y" to the `totals` section in BOTH GOLDEN-mirrored builders (src/lib/receipt.ts ESC/POS + src/lib/receipt-lines.ts StarXpand bitmap), thread creditApplied(already fetched)+rewardEarned(getOrderRewardSummary)+rewardLabel via ReceiptOrder in kitchen-receipt-payload.ts. ⚠️ GOLDEN pipeline — REQUIRES a physical print test on the Star TSP143 before trusting. i18n receipt.customer.paidWithReward/earnedReward ×38.

## Minor: "You unlocked 50% off" toast fires on a gift-card-only cart (Luigi 2026-06-29)
The promo-unlocked toast ("You unlocked 50% off entire menu!") shows even when the cart is ONLY gift cards, which are correctly EXCLUDED from the discount (subtotal stays full — discounting gift cards = arbitrage). The discount math is right; only the toast is misleading. Suppress the unlocked-toast / promo nudge when no cart line is actually eligible for the promo (e.g. gift-card-only cart). Low priority, display-only.

## CORRECTION to the toast note above (Luigi 2026-06-29)
The gift card is NOT excluded from discounts — it's a normal menu item and DOES get the 50% once the code is applied. The "50 % off entire menu" promo is autoApply=false / couponCode=VIPSKOOLJAN (code-gated), so it correctly didn't discount a cart with no code entered. The REAL bug = the "You unlocked 50% off" nudge/toast fires for a code-gated promo even when its code hasn't been applied, implying it's active when it isn't. Fix: suppress the unlocked-toast/nudge for autoApply=false (hidden/coded) promos until the code is actually applied. Tie into the nudge/highlight rework + verify across promo types (standing rule).

## Consider: exclude gift-card (or specific categories) from Reward Dollars EARNING (Luigi 2026-06-29, low priority)
Gift cards are normal menu items in Luigi's setup, so buying a $20 gift card earned $1.00 (5%) in Pizza Bucks — a small store-credit loop (buy store credit → earn store credit). Most reward programs exclude gift cards from earning. OPTIONAL: add a per-item/per-category "no rewards earning" flag (mirror of how some stores exclude gift cards from discounts). Luigi's call — only if he wants to stop the loop. Earn basis today = subtotal − discounts across all items.

## FEATURE: Reward Dollars — exclude items/categories + "sign up to earn" banner (Luigi 2026-06-29)
TWO requests on /admin/rewards (only relevant when rewardsEnabled):

1) **Exclude specific items/categories from the reward program.** Owner picks items and/or categories that are NOT part of Reward Dollars. Decisions to confirm with Luigi: earning-only exclusion (those items' subtotal doesn't count toward earn basis) vs ALSO spend-exclusion (credit can't pay for them — harder, since credit is a whole-ORDER payment, not itemized). Recommended v1: EARNING exclusion (addresses the gift-card loop), category + item granularity. Schema: side table or JSON list of excluded categoryIds/itemIds on Restaurant (sparse — keep off hot tables). Wire into reward-ledger.awardForOrder + reward-earn.projectOrderEarn + awardEarnRulesForOrder basis (subtract excluded lines). MUST show on the CUSTOMER account page ("X and Y don't earn {rewardName}"). i18n ×38 + HelpTip.

2) **"Sign up to earn" banner/message toggle.** Since guests can't earn/spend (spend requires sign-in), give the owner a toggle in reward settings to show a customer-facing banner/message on the order page prompting guests to sign up to start earning {rewardName}. Only show to logged-OUT customers when rewardsEnabled. Schema: Restaurant.rewardSignupBannerEnabled (bool) + optional custom text. Customer display on /order/[slug] (OrderingPageClient) for guests. i18n ×38.

### DECISIONS (Luigi 2026-06-29): exclusion = EARNING-ONLY for v1 (excluded items/categories don't earn; bucks can still pay toward the whole order). Both features queued for a fresh session, AFTER the 🔴 launch tests. Banner = guests-only, shown when rewardsEnabled.

## FEATURE: VIP group — add members by PHONE too (Luigi 2026-06-29)
The group "Add members by email" field rejects phone numbers ("Enter at least one valid email"). Luigi wants to add members by EMAIL or PHONE ("we need both"). BLOCKER: the system matches VIP members (for both member-specials AND scheduler credit grants) by customerId/email only — there is NO phone-based matching, so a phone-only member is currently inert (credit cron skips them by design, specials don't match them). To make phone membership meaningful, FIRST add phone-normalized customer matching (see the existing `Customer.phoneNormalized` TODO), then let the group add-field accept phones + resolve them to customers. Until then, either keep email-only OR clearly warn that phone-only members won't receive credit/specials. Decide with Luigi.

## FEATURE: per-GROUP member label for VIP Specials (Luigi 2026-06-29)
Today the "What do you call your members?" label is ONE restaurant-level field (Restaurant.vipMemberLabel, schema:327) → every group/email says the same thing ("As a {label} at your restaurant…"). Luigi wants EACH group to have its own label so different groups make sense (e.g. "Bruce Trail Staff" vs "VIP Pizza Club").
PLAN: add `CustomerGroup.memberLabel String?` (nullable; BOTH Neon branches via scripts/push-schema-to-both.ts). Group "Manage" page gets its own "What do you call your members?" field (reuse admin.customerGroups.memberLabel* i18n keys); restaurant-level field stays as the DEFAULT/fallback + used for individual (non-group) specials. Wire `vip-notify.ts` (notifyGroupOfSpecial) to use `group.memberLabel ?? restaurant.vipMemberLabel ?? default` — this also flows through the scheduler's discount_resend automatically. i18n: add a "(overrides the default for this group)" hint ×38. No new email keys needed (email.ts already takes memberLabel param).

## Test #18 ghost-ring — partial result + RE-TEST needed (Luigi 2026-06-30 ~00:37)
Run was CONFOUNDED: 2nd device was an iPhone on an OLD TestFlight build (FEEFREE v1.9, predates the ghost-ring fix 16b77b18) + iOS screen-off push isn't wired (registered 0 push tokens). What we DID confirm: logging in on device 2 logged out the S23 AND the S23 unregistered its own push token → kitchen-push-tokens table went 1→0, so the superseded S23 can't ring on screen-off. The "ghost ring" Luigi heard was on the iPhone (active device, old build, FOREGROUND ring), not the screen-off push path. RE-TEST with BOTH devices on the CURRENT Android build (S23 + SM-T350 tablet, v2.8/2.9): log in A→B (expect 1 token = B, A's gone), lock A + place order → A SILENT, B rings; re-login A → A rings. Use scripts/kitchen-push-tokens.ts to confirm the 1-token invariant at each step. iPhone is NOT a valid surface until the iOS push + current build ship.

## TASK: real pizza-builder on the DEMO restaurant + #1-pizza-builder marketing (Luigi 2026-06-30)
1) DEMO account (prod, slug `fee-free-demo-restaurant`, customer/marketing-facing): make its pizza items (Margherita / Pepperoni / Quattro Formaggi) REAL pizza-builder items (pizzaRole + pizzaConfig + crust/sauce/cheese/toppings modifier groups) so prospects experience the true builder incl. HALF/HALF. Copy the config from Luigi's store (info@luigislasagna.com / luigis-lasagna-pizzeria) — Luigi named "Monday Medium 1 Topping Special" as the source (verify it's a full builder pizza; if not, use a fully-configured BYO pizza). CROSS-restaurant copy → existing copy-settings is WITHIN-restaurant, so a script is needed; Luigi runs it on prod (prod writes blocked for assistant).
2) MARKETING: position FeeFree as the "#1 custom pizza builder platform for independent pizzerias" — add to homepage/features/SEO pizza pages. i18n as required.

## DONE 2026-06-30: refund-to-wallet on full refund (commit 97d97586)
reward-ledger.refundForOrder() returns spent credit + claws back earned on a FULL card refund (idempotent, after()-dispatched from api/orders/[id]/refund). Reward money lifecycle now complete (reject/auto-reject/refund all handled). Was the 🟡 "Refund-to-wallet on FULL refund" item.

## Reward Dollars — deferred / follow-up (2026-06-30)
- [ ] **Reward Dollars expiry cron** — `Restaurant.rewardExpiryDays` exists but isn't enforced. Needs FIFO lot-aging (track each earn's date, expire oldest-first, write `reason:"expire"` ledger rows, idempotent per lot+day). Design decision, not a quick add — own task.
- [ ] **Verify earning-exclusion on device** (after deploy): in Reward Dollars settings, exclude a category (e.g. Gift Cards) → place an order with an excluded item + a normal item as a logged-in customer → confirm earn only on the non-excluded portion. Customer account page should show "Some items don't earn …".
- [ ] **Verify per-group VIP label** (after deploy): set a group's "What do you call your members?" → send that group a special → email should use the group label, not the restaurant default.
- [ ] **Clean up the live "Schedule Tester" VIP automation** (still granting $5/day on prod): `npx tsx scripts/run-on-prod.ts scripts/_cleanup-vip-schedule-test.ts luigis-lasagna-pizzeria "Schedule Tester" sched-test@example.com`

## UX notes (Luigi, 2026-06-30)
- [ ] **"Empty cart" button throughout the checkout stages** — add a one-click clear-all-items button available in the cart drawer AND on the checkout screen(s). Today you can only remove items one at a time, which is tedious (surfaced while setting up a promo test). Scope: a small "Empty cart" / trash control on the cart slide-over + checkout, with a confirm guard.

- [ ] **Import-to-Try restaurants shouldn't count as real restaurants (superadmin)** — Luigi 2026-06-30. The "try-*" restaurants created via the main-page GloriaFood menu test-build (Import-to-Try growth engine, e.g. "Ontario Stacked Full Menu 2025.11" → /order/try-...) are being counted in the Super Admin → All Restaurants stats (TOTAL / FREE / MRR) and listed inline with real signups. They should be bucketed separately like the existing TEST (DEMO-*) category: detect them (slug prefix "try-" or an `isImportTrial`/source flag) → exclude from real-restaurant counts → add a filter tab ("Try imports") + own count tile. Source: project_import_to_try.

## DONE 2026-07-03: EU VAT compliance — VIES + Option A + reverse-charge invoices (commit c375650c, Fabrizio cmr1ty0lc)
Shipped: src/lib/vies.ts (official VIES REST check, fail-soft on downtime), RestaurantBillingProfile.taxIdViesValid/taxIdViesCheckedAt (BOTH Neon branches), VIES check on billing-profile save + "Verify now" re-check in Billing → Fiscal details, Option-A gate on plan + add-on checkout (EU restaurants need a VIES-valid VAT number before any PAID subscription; stays free otherwise), invoice shows 0.00% tax row + Art. 44 / Directive 2006/112/EC reverse-charge note when validated. Decisions (Luigi 2026-07-03): issuer = Fee Free Ordering Inc. (already platform-issued); GST/HST 809409832RT0001; Option A (no EU OSS registration at 0 sales).
- [ ] **OWNER (Luigi):** Superadmin → Settings → Company — enter legal name "Fee Free Ordering Inc.", tax id "GST/HST 809409832RT0001", and the REGISTERED BUSINESS ADDRESS (still missing; invoices render without an issuer address until then).
- [ ] **OWNER (at Stripe Live setup):** enable Stripe Tax to charge Canadian restaurants GST/HST by province (runbook item; EU handled by the reverse-charge path above).
- [ ] Verify on prod after deploy: save an Italian profile with a real P.IVA → badge turns "VIES: valid"; invoice for a validated EU profile shows the 0% row + Art. 44 note; a Canadian profile is untouched.

## DONE 2026-07-03: invoice completeness vs GloriaFood/Oracle sample (commit 35c6327c)
Luigi compared our invoice to the Oracle one — gaps closed: PlatformSettings.companyRegistryNo + companyWebsite (both branches; free text incl. label), Customer number + Payment reference in header, Nr/Qty/Description/Unit-price/Amount table with restaurant ID+name+address inside the line, Sub-Total + ALWAYS-visible 0.00% tax row + Total, issuer legal footer (name/email/website/registry/GST-HST/address) + license-terms note. 10 i18n keys ×38. Visually verified on dev (seeded invoice, EU VIES-valid profile: 0% row + Art. 44 note render).
- [ ] **OWNER (Luigi):** Superadmin → Settings → Company now also has "Company registry number" (enter your federal/provincial **Corporation Number** from the certificate of incorporation, e.g. "Corporation No: 1234567-8") and "Website". Fill together with the registered address.

## DONE 2026-07-03: reseller VAT numbers get live VIES validation (commit fe602e4c)
Luigi: the VIES check MUST also cover resellers. ResellerProfile.companyVatViesValid/CheckedAt (both branches), parseViesVatNumber() in vies.ts (self-prefixed numbers, GR/EL both accepted, non-EU → null), PATCH /api/reseller/branding validates on save (fail-soft) + POST recheck, badge + "Verify now" on Reseller → Branding → Imprint. Verified end-to-end on dev vs the LIVE VIES service (red badge on a lapsed 2023 P.IVA, green on IE6388047V). Addendum comment posted on cmr1ty0lc telling Fabrizio to save his P.IVA and confirm the badge.

## QUEUED 2026-07-03: EU non-Union OSS + Stripe Tax — charge local VAT to non-VIES EU clients (Fabrizio cmr1ty0lc follow-up; committed "soon" in the thread)
Full GloriaFood model: VIES-valid B2B = 0% reverse charge (LIVE); non-VIES EU client = charge their country's VAT (22% Italy etc.) as B2C instead of blocking the purchase.
- [ ] **OWNER (Luigi):** register Fee Free Ordering Inc. for the EU **non-Union OSS** scheme in ONE member state (Ireland recommended — English-language portal; registration itself is FREE; no fiscal intermediary required for the non-Union scheme). Get an accountant to confirm + optionally handle the QUARTERLY OSS return (DIY possible; agents roughly €300–1,500/yr).
- [ ] **OWNER (Luigi):** enable **Stripe Tax** on the platform Stripe account (pay-as-you-go: ~0.5% of transactions where tax is calculated; subscriptions via Billing ~0.7%; only applies to the transactions that get taxed — $0/mo when nothing qualifies).
- [ ] CODE (after both): keep VIES-valid → 0% Art-44 path unchanged; replace the euVatSubscriptionBlock 403 for non-VIES EU with checkout that sets automatic_tax (customer-country rate); store tax amount on SubscriptionInvoice from the webhook; invoice renders the REAL rate/amount row instead of the hardcoded 0.00%; then post the "it's live" update on cmr1ty0lc.

## DONE 2026-07-03: promo "Get it Now" qty steppers + grouping confirmed (commit cbac69f7, cmqtmfp2n)
Simple eligible items on the promo screen flip "+ Add" → −/qty/+ stepper once in cart (options items keep Customize); quick-add now INCREMENTS one plain cart line instead of stacking duplicates; new onRemoveItemDirect + cartQuantities plumbing (plain lines only: no variant/mods/notes/bundle/pizza). Category grouping verified working (PIZZAS/PASTA/SALADS headers). 2 aria strings ×38 (EN "Remove a {name}" — avoid "one {name}", parity audit ICU false-positive). Browser-verified on dev seeded 20% percentage_off w/ groups: stepper 1→3→2, ONE cart line ×2 $25.98, −$5.20 promo named in breakdown. Completion reply posted on the report (in-app; verify EMAIL = Luigi's UI button). Fabrizio still owes on-device confirm.

## DONE 2026-07-03: kitchen tiles reverted to two-line layout, ZIP dropped, names = reservation size (commit 7125c853, Fabrizio screenshots)
Supersedes the 2026-07-02 one-line GloriaFood-exact experiment (771a07bf). Delivery w/ show-name ON: name on top (text-base bold = reservation size) + address on its OWN muted text-sm line ("street №, city" — NO postal code). Pickup/dine-in/take-out/address-only leads text-sm → text-base. Display-only in OrderRow; ring/print/poll untouched. DOM-measured on dev kitchen: name 16px/700 over address 14px/400 gray, two lines, ZIP absent, lowercase seeds capitalized. Reply posted on cmqsn52d2. **Still needs on-device look (S23 + tablet).**

## DONE 2026-07-03: kitchen tile lead text-lg + NEW "what leads the delivery tile" setting (commit 62af6ca3)
Follow-ups on the two-line revert: (1) bold lead line text-base → **text-lg** ("slightly larger", Luigi); reservation tile name set to the same text-lg in BOTH densities so all names read one size. (2) NEW Restaurant.kitchenDeliveryLead "name"|"address" (default name; both branches) — Settings kitchen-display delivery card gets a sub-choice (visible only when show-name is ON) picking WHICH line goes on top; formatting fixed (top always bold text-lg, bottom always muted text-sm). Saved via /api/restaurants/profile, returned by the kitchen 4s poll → applies LIVE without app restart (verified: flipped both ways in preview, 18px/700 top + 14px/400 bottom each way). 6 i18n strings ×38 (parity 5295). Display-only; ring/print/poll untouched. On-device look still pending.

## 🔒 LOCKED 2026-07-03: kitchen display / tiles / zoom — tag kitchen-display-verified-2026-07-03 (8f194f2d)
Luigi: lock everything about kitchen tiles + order app display; no regressions. Locked set: two-line delivery tiles (bold text-lg lead, muted text-sm second line, street+city NO postal code, displayCaps), kitchenDeliveryLead "name"|"address" setting (live via 4s poll), ALL customer names text-lg (orders + reservations, both densities), per-device zoom Standard/1.2×/1.5× (localStorage kds-zoom; calc(100dvh/zoom) height compensation; hydration-safe load — lazy initializer breaks SSR, don't "simplify" it back). Zoom needs NO app rebuild (WebView renders the web display). Do not change any of this without Luigi explicitly asking; revert to the tag if a regression ships. On-device confirm (S23 + tablet) still pending; Fabrizio recheck outstanding on cmqsn52d2.

## ✅ 2026-07-03: kitchen display lock is now DEVICE-CONFIRMED
Luigi verified everything on the kitchen apps including the zoom options; Fabrizio confirmed the tile layout in-thread ("Now it's perfect, great job!"). Tag kitchen-display-verified-2026-07-03 is a hardware-verified revert point. Remaining on cmqsn52d2: Fabrizio to try zoom + the lead-line setting, then mark FIXED (human-gated; the verify EMAIL button is Luigi's).

## DONE 2026-07-03: checkout coupon "Apply" button fixed (commit dec39659, Fabrizio cmqtllluu)
The checkout Apply was wired `onClick={applyCoupon}` — the click EVENT landed in applyCoupon's optional codeArg and `.trim()` on a MouseEvent threw BEFORE the fetch → silently dead button (cart's `() => applyCoupon()` worked, hence "works in cart, not at checkout"). Fixed both sides: CheckoutModal uses an arrow wrapper; applyCoupon type-guards codeArg (`typeof codeArg === "string"`) so any call-site shape is safe. Browser-verified with seeded TEST1 (10%): checkout → Promo code → TEST1 → Apply = −$3.90 on $38.97 instantly. Reply posted on the report; Fabrizio to retest.

## DONE 2026-07-03: honest "code valid but no effect" coupon toast (commit 46100cb4, Luigi 20OFF test)
Luigi applied 20OFF to a GIFT-CARD-ONLY cart on prod: $0 discount was CORRECT (gift cards promo-excluded per his own setting) but the Apply button flashed silently — the pending-coupon resolver only spoke on applied/blocked, never on valid-but-zero-effect. Now: apply-promos response records which code the eval included (lastEvalCouponRef, per-request capture → no stale-response false message); neither-list + code-evaluated → info toast "{code} is valid, but it doesn't discount anything in your cart right now — some items (like gift cards) may be excluded, or a minimum may not be met." ×38 (parity 5299). Verified both ways on dev: excluded-only cart → info toast + $0; mixed cart → applied toast + both promos discount ONLY the non-excluded item.

## DONE 2026-07-03: per-item "You saved" badges in the CART drawer (commit 57b88ef5, Fabrizio cmqv33v2o)
Badges only showed at checkout; now the cart drawer lines carry the identical "👍 You saved X" badge (same lineKey attribution as CheckoutModal; whole-cart promos stay in the totals). Reuses checkout.youSaved ×38 — no new keys. Verified: mixed cart shows badge on the 20%-covered dish only, badge sum == Discount row. Reply posted; Fabrizio to retest.
ALSO ANSWERED (Luigi stacking question): checkout showing 50% (Copy) + 20OFF together is CORRECT per config — both 50% promos are stackingRule "exclusive" (one wins, the loser explained in the yellow notice), while "20% OFF EVERYTHING" (20OFF) is stackingRule "master" = combines with everything, so it stacks on the winning exclusive (70% total). If 20OFF shouldn't stack, edit that promo and change its combinability from master to standard/exclusive.

## DONE 2026-07-03: guided promo "Get it Now" → step-by-step wizard (commit 6cfd2385, Luigi)
GuidedPromoModal (bogo/buy_n_get_free/free_dish_meal/fixed_combo/percentage_combo) rebuilt GloriaFood-style: ONE group per step ("Step N of M"), single-pick steps auto-advance, LAST pick auto-completes into the cart (engine applies discount), finished steps = tappable chips showing the picked item (jump back + change, picks preserved), multi-pick steps get explicit Next/Back. 3 strings ×38 (parity 5302). Browser-verified full loop on a 2-group BOGO. Semantics (GuidedPromoPick output, free-slot flags, engine contract) unchanged.
- [ ] FOLLOW-UP: apply the same stepper ideology to **BundleComposerModal** (meal_bundle / meal_bundle_speciality) — its slots still render all-at-once. Same pattern: step per slot, chips, auto-advance. (FreebiePromptModal is single-choice already = fine.)
- [ ] DEV-SEED note: demo "BOGO Pizza / Pasta" promo is delivery-zone-restricted (won't discount pickup carts) and was autoApply:false w/ no code (inert) — flipped autoApply on dev for testing.

## DONE 2026-07-03: promo-flow audit — every Get-it-Now aligned to the step-wizard standard (commit 964ad420)
Type-by-type pass (Luigi): BundleComposerModal (meal_bundle/_speciality) rebuilt as the same stepper (one slot/step, auto-advance, last pick auto-ADDS the bundle, chips to revisit; reuses guidedPromo wizard strings — no new keys); PromoDetailModal bundle branch now CLOSES on add (pre-existing gap); payment_reward joined the whole-cart "Start adding items" CTA group + shows the human payment name via formatPayment (was raw "online_card"). Audited-as-fine: FreebiePromptModal (single decision), fixed_cart / groupless % (CTA), free_delivery (switch CTA), % with items (steppers + grouping). ComboComposerModal = menu combo-ITEM builder, not a promo — untouched (could get the same treatment as a menu-UX task if wanted). Browser-verified: 3-slot bundle walked, auto-added $29.99 + modal closed; payment_reward CTA + "Card online" wording.

## DONE 2026-07-03: category header style choice (commit faf443fa) + promo escape hatches / leave-guard (commit 95cb5ee7)
(1) Theme → new "Categories without a photo show as" sub-choice under Show-category-images: Color banner (default, unchanged) or Simple header (classic). Photo categories always banner; toggle OFF = classic everywhere. ThemeSettings.categoryNoImageStyle (JSON, no schema change). Verified both ways on dev. (2) Every promo detail screen now has "See full menu" + "Go to cart (n)" escape buttons (primary CTAs merged into the same footer); guided + bundle wizards show an inline "Leave this deal unfinished?" confirm when closed MID-build (untouched/complete closes silently; Keep building preserves picks). 9 strings ×38 (parity 5311). Both browser-verified.

## DONE 2026-07-03: reward-credit type gated (09e43e0b) + buy_n_get_free triple fix (626d2aac)
(1) Grant Reward Dollars promo type hidden from the wizard picker when rewardsEnabled OFF (visible if editing an existing one) + server 400 backstop on create. Verified both toggle states on dev. (2) buy_n_get_free MONEY FIX: customer-CHOSEN (isFreebie-tagged) free items are freed FIRST — a cheaper untagged item added later can no longer steal the discount (Luigi's $25-pizza repro); the strategy dropdown (cheapest/most-expensive) now only decides UNTAGGED carts. 6 engine unit tests incl. the exact regression (392 total). (3) Count-aware wizard copy ("Pick 3 items — then choose your free item", chips/steps say "Pick 3 items") + multi-pick groups accept the SAME item ×N (tap adds, ×N badge, − drops one; mirrored in bundle composer). 4 strings ×38 (parity 5315). Browser-verified full walk on a seeded replica.
OWNER NOTE: on manually-built carts the promo "Discount Strategy" dropdown picks the freebie — Luigi's promo = "cheapest"; flip to "most expensive" in the promo editor if preferred.

## 2026-07-03 — Missed-order call: ROOT CAUSE FOUND + self-diagnosing test button (Claude)
- Twilio call log PROVES delivery: 3x "Completed 24 Secs" (full alert message played — almost certainly into voicemail after carrier screening) + several "No Answer" (Twilio rang, handset silent). Cause: calls FROM the toll-free +18886188765 with no STIR/SHAKEN attestation get spam-silenced by Canadian carriers/handsets.
- OWNER ACTIONS:
  1. `git push origin main` — 2 local commits: b6cf5f8c (alert-phone field refuses undialable formats + shows "Will dial: +1…") and 7bdc53d8 (test button now polls Twilio ~9s after placing and shows the REAL verdict — status + error code — in a panel under the button).
  2. Twilio → Phone Numbers → Buy a number → Canada → Voice → area 647/416 (~US$1.15/mo). Then Vercel → Settings → Environment Variables → FFOS_TWILIO_FROM_NUMBER = the new +1647… number → redeploy. Toll-free 888 stays for future SMS (its toll-free verification was REJECTED — resubmit before using SMS features).
  3. Check voicemail on 647 669 0808 — the "Completed 24 Secs" test calls likely left the alert message there. Also check the phone's "Silence unknown callers" / carrier Scam Shield settings.
- 2026-07-03 UPDATE — CONFIRMED by differential test: LANDLINE rings perfectly, cell phones (Luigi + staff, same test) stay silent. Mobile carriers spam-silence the unattested toll-free FROM number; landlines do not filter. Interim: keep the LANDLINE as alert number (works today). Permanent: local Canadian voice number -> FFOS_TWILIO_FROM_NUMBER swap (step 2 above). The 4-calls-in-7s burst was manual test clicks, but the cron now also enforces one call per restaurant per 3 min (regression-safe hardening, commit pending gate).
- NICE-TO-HAVE (deferred by Luigi 2026-07-03): inbound handling for the new alert caller number (289) 768-7778 — callbacks to it currently hit Twilio's demo recording. Fix when wanted: TwiML Bin with <Response><Dial>+1landline</Dial></Response> set as "A call comes in" on the number. FFOS_TWILIO_FROM_NUMBER=+12897687778 is LIVE in Vercel (Luigi updated + redeployed 2026-07-03).
- 2026-07-03 LATER UPDATE: env swap to +12897687778 CONFIRMED in Twilio logs (From=289) but cells STILL silent (multiple handsets, no Recents trace; landline fine; manual person-to-person calls ring). New prime suspect: handset/carrier spam engines (Samsung Hiya etc.) filtering unknown robot calls. Pending decisive test: save 289 number as CONTACT on the S23 -> retest. OWNER ACTION (do regardless): Twilio Trust Hub Business Profile (Fee Free Ordering Inc. + GST 809409832RT0001) + attach SHAKEN/STIR + Voice Integrity to the 289 number so alert calls arrive attested/labeled. If contact-test still fails: file Twilio support ticket w/ SIDs CA82666c15f5ddae4a51f9a6c508840032 + CAcdb3fe4aaf6ab8b69e38b79376e9c7f1 (carrier-response trace).
- 2026-07-03 FINAL DIAGNOSIS UPDATE: all failing cells are ROGERS (Fido=Rogers). VoIP landline <-> Twilio works BOTH directions (greeting plays); Rogers cells fail BOTH directions on BOTH numbers (289 + 888). => Rogers <-> Twilio-partner interconnect/routing failure (precedent: Rogers-Iristel disputes), started recently ("worked in June"). Tickets drafted for Twilio (with SIDs) + Rogers. Cross-check pending: dial 289-768-7778 from a Bell/Telus cell. LAUNCH-SAFE: landline alert path fully working.
- 2026-07-04 CLOSING UPDATE: contact-saved test STILL fails -> handset filtering ruled out. June 13-14 Twilio logs show human-answered calls to the same Rogers cell (4-28s durations) -> break window = June 14..July 3, network-level, both directions, Rogers only. Code audit clean (only TwiML message changes in the window; cannot affect ringing). REMAINING = owner files 2 tickets (texts in chat): Twilio (SIDs + before/after) + Rogers (routing escalation). Landline alert = working; launch NOT blocked. Also still pending: check Twilio bell notification + toll-free rejection date; Bell/Telus cross-dial of 289-768-7778.
- 2026-07-04 RESOLVED: missed-order calls to Luigi's cell WORK now. Root cause = the handset's stale network/VoLTE registration (restart + making an outbound call forced re-registration). NO Twilio/Rogers tickets needed. Keep: local FROM number +12897687778 (better answer rates + clean reputation), 289 greeting TwiML Bin, self-diagnosing test button. If a customer ever reports the same symptoms (Twilio "No Answer"/"Completed" + silent phone + call-failed dialing back): have them restart the phone and place a call first.
- 2026-07-04 HARDENING NOTE — RESOLVED (was stale): /api/orders DOES validate scheduled slots against service opening hours ("Weekly-hours backstop for SCHEDULED orders", code outside_opening_hours) — shipped with the split-hours work. Verified in code 2026-07-05.

## 2026-07-04 overnight batch — 3 Fabrizio/company items (Claude, ~3:30 AM)

### Item 1 — CORPORATION REGISTERED ✅ (data entered) + OSS checklist
DONE tonight: extracted from Certificate + Articles of Incorporation (OneDrive\Desktop\Fee Free Ordering\):
  - Legal name: FEE FREE ORDERING INC. | Ontario Corporation No. 1001666063 | incorporated 2026-07-03
  - Registered office: 17 Commercial Street, Unit Lower, Milton, Ontario L9T 2H6, Canada
  - WRITTEN TO PROD PlatformSettings (script set-company-registration-2026-07-04.ts): full address (was missing unit+postal) + Corporation No. (was empty). Every subscription invoice now has the complete legal block. Check any invoice to confirm.
OSS (what Fabrizio's cmr1ty0lc thread needs so non-VIES EU clients can PAY) — owner steps for tomorrow:
  1. Pick the EU "member state of identification" for non-Union OSS. RECOMMEND IRELAND (English portal, non-EU friendly): Irish Revenue ROS -> "Non-Union OSS" registration.
  2. You now HAVE everything the application asks for: legal name ✓, registered address ✓, country of establishment (Canada) ✓, national tax number (GST/HST 809409832RT0001 or OCN 1001666063) ✓, website ✓, contact email ✓, incorporation certificate PDF ✓. Also needed: a declaration you have no EU establishment (true) + date of first expected EU supply + bank details for refunds (your business account, SWIFT/IBAN accepted).
  3. Submit; they issue an "EU" VAT ID (format EUxxxyyyyyz), usually days-weeks.
  4. Give me that EU VAT ID + effective date -> I build task #12 (charge each EU client's local VAT at checkout, invoices show VAT line, quarterly OSS return export) and flip the Option-A block off.

### Item 2 — Category banner choice: ALREADY SHIPPED as task #20 (verify + reply tomorrow)
Fabrizio's exact ask = what we shipped 2026-07-03: Admin -> Website -> Theme -> "Show category images" toggle.
  - OFF -> plain classic name-only headers (his "exactly how it was until a few days ago").
  - ON -> categories WITH an image show the image banner; imageless ones use the sub-choice (gradient band vs plain).
TOMORROW: (a) confirm per-category image UPLOAD exists in Menu Setup (category edit) — if missing, that is the one gap to build; (b) tell me which report his comment is on and I post the where-to-find-it reply.

### Item 3 — SOLD-OUT CART BYPASS: FIXED tonight (server guard) ✅
Bug confirmed: /api/orders never checked isSoldOut — an item added to cart before the kitchen flagged it sold out ordered fine on the return visit.
  - Server now rejects with code item_sold_out + item name (normal items AND combo parents); customer sees localized "{name} just sold out — remove it from your cart to continue" (×38, parity 5331).
  - Verified on dev end-to-end: flagged Penne Arrabbiata sold out -> POST /api/orders -> 400 item_sold_out. Reverted flag.
  - NICE-TO-HAVE queued: auto-PRUNE sold-out lines from a restored cart on page load (with a toast) so the customer never even hits the error; and check bundle/combo COMPONENT items for sold-out too (currently only the parent).
  - Reply to Fabrizio pending: tell me which report (or say "find it" — search body for "sold out").
- LUIGI NOTE (2026-07-04): /admin/delivery (Delivery Zones editor) currently renders on Leaflet/OpenStreetMap — should use GOOGLE MAPS for all stores, consistent with the customer-facing pages (platform Google Maps key already exists in PlatformSettings.googleMapsApiKey + restaurant override). Scope: swap the zones map to Google Maps JS (circle overlays w/ drag-to-resize, re-locate control), keep all zone CRUD behavior identical.
- 2026-07-04 CORRECTION (supersedes the "zone transit" notes above): a delivery zone's Std. Time = the TOTAL estimated delivery time (prep + travel) for that zone and CORRECTLY overrides the service default Estimated time (Luigi ruling). The morning max()/"min drive" change was REVERTED; the real fix = admin-side explanation now under the Std. Time field in both zone forms (new + edit, ×38). Fabrizio's wrong replies on cmqqxerxs + cmqt99i8s were deleted and replaced with the corrected explanation (set zone Std. Time to realistic 30–60 min).
- LUIGI NOTE (2026-07-04, ord #238064650): "Card on delivery" order's STAFF email showed blue "Paid online" chip + "Collected" — staff could skip charging the card at the door. Root cause: fireOrderNotifications computed paidOnline as `paymentMethod !== "cash"` (card_in_person wrongly counted as paid). FIXED same night: paidOnline = card|paypal|reward_credit only, and the unpaid chip now says WHAT to collect ("To collect — card" / "To collect — cash") on both the new-order and accepted staff emails. Customer emails/receipts/status page were already correct (they key off paymentStatus).
- LUIGI NOTE (2026-07-05): "Place order" can be clicked multiple times while the checkout POST is in flight (screen sits on Checkout until the server responds) — has produced 2-3 duplicate real orders before. Requirement: make duplicate submission IMPOSSIBLE (client button latch + server idempotency key on /api/orders).
- LUIGI NOTE (2026-07-05): Competitor watch — https://www.town.club/ is doing something very similar and is ahead (4 co-founders + 1 CTO, likely hundreds of restaurants). Their owner **Taylor Brewster** just signed up at feefreeordering (competitive scoping / traction signal). Task: intense scan of ALL their marketing pages + FAQ as lead designer → what to add/improve in our marketing + program. (Analysis in progress 2026-07-05.)
- OWNER PIPELINE (2026-07-05): CIBC business account for FEE FREE ORDERING INC. applied — ready in a few days. It UNBLOCKS: (1) Ireland non-Union OSS registration at https://www.ros.ie/vatoss-web/vatoss.html (form answers all confirmed: legal name FEE FREE ORDERING INC., 17 Commercial Street / Unit Lower / Milton, Ontario L9T 2H6 / Canada, website feefreeordering.com, established-in-union NO, tax ref 809409832RT0001 issuing country Canada, trading name Fee Free Ordering; bank = new CIBC account number + SWIFT CIBCCATT, account name FEE FREE ORDERING INC.) → EU VAT number → task #12; (2) Stripe LIVE payouts for the platform (live-keys walkthrough waits for this too). Search Console: DONE 2026-07-05 (sitemap submitted, /gloriafood-alternative indexing requested). Upstash env vars: verified present.

## RED-TEAM MONEY-PATH REVIEW (2026-07-06) — 9 confirmed findings, real ones FIXED (commit f7a0624c)
Adversarial workflow over the pizza pricing engine + promo delete-guards shipped 2026-07-05. FIXED + shipped:
- **Pizza tamper theft (whole-supersedes-half dedupe)** — removed. A crafted checkout body could stack whole + (L.H) + (R.H) of one topping and pay for only the whole while the kitchen made all three. `isHalf`/`isLight` come from the client modifier NAME, so the dedupe was a discount lever. Now every printed line is charged (no-op for honest carts — builder emits one placement per topping).
- **Split sauce/cheese preview≠charge** — computePrice added the whole-sauce/cheese price ON TOP of the two halves; serializer sends only the halves → customer charged LESS than shown. Preview now mirrors the serializer branch exactly.
- **halfToppingMultiplier > 1** — engine fell back to 0.5, route clamped to 1.0 → divergence. Engine clamps [0,1] to match; MenuClient clamps on save.
- **Standalone modifier DoS** — no length cap on rawMods (combo/bundle cap 60/20). Capped at 120.
- **Promo delete-guard: variantIds blind** (HIGH) — a size-only promo (20%-off Large) was invisible to the guard; deleting the dish cascade-deleted the variant with no 409. promosReferencing now scans variantIds; item/category DELETE pass them.
- **Whole-menu DELETE had NO promo guard** — added referenced_by_promos 409 + ?force + translated "Delete anyway?" (admin.menus.deletePromoBlocked ×38).
- **findDeadPromoIds mass-quarantine when no scheduled menu** — now serves all promos when servedMenuId is null (page serves dishes restaurant-wide there).

DEFERRED (flagged, not shipped):
- **dedupe-categories merge** (POST /api/admin/menu/dedupe-categories) hard-deletes duplicate items + loser categories with NO promo guard (LOW severity — survivor keeps an equivalent dish, so only variant/id-specific promo refs break). Add a promosReferencing skip/warn like the other delete routes.
- **BUSINESS QUESTION for Luigi:** in the PER-OPTION pizza model (extraToppingPrice=0), a topping marked "Light" is charged $0 even if its option has a real price (e.g. a $2 premium topping → free when light). Food-consistent (kitchen makes light, customer pays light), but is light SUPPOSED to be free for paid per-option toppings? If not, that's a pricing-model change (affects everyone, not a tamper bug).

## LIGHT TOPPING = PRICE-NEUTRAL — RESOLVED (2026-07-06, commit 03a1ac51)
Luigi clarified: "LIGHT topping does not mean it's free, it just means there's no additional charge. Once a topping is added (it's paid) they can go LIGHT and it shouldn't charge or discount additionally."
→ Removed isLight from the pricing engine entirely. A light topping is now priced + credited exactly like a normal one; the ", Light" suffix is a kitchen label only (so the kitchen puts less on). The ×N count stepper remains the only paid multiplier. Also killed the ", Light" name-string tamper class as a side benefit.
NOTE (not acted): `extraQuantityMultiplier` in pizzaConfig is defined but never applied to price — "Extra" is amount-only / price-neutral like Light, consistent with the model. Left as dead config. Flag if Luigi ever wants Extra to upcharge.

## SECURITY HARDENING — stored-XSS + HTTP headers (2026-07-06, commit 5fa6300d)
Audit found + FIXED: (1) JSON-LD `</script>` stored-XSS on hosted/white-label pages (owner-set name breaks out) → shared `src/lib/safe-json-ld.ts`, applied to all ~16 ld+json emits; (2) email templates (OrderRefund/OrderCanceled) raw-HTML restaurant name → escapeHtml; (3) hosted-site CTA `javascript:` href → sanitizeExternalHref (render + write); (4) GA/Pixel inline-JS IDs JSON.stringify'd (defense-in-depth). Headers in next.config.ts: nosniff, Referrer-Policy, HSTS (2y, no preload), Permissions-Policy (camera/mic off, payment+geolocation kept), X-Frame-Options SAMEORIGIN + CSP frame-ancestors 'self' EXCEPT /embed + /order (verified live via curl). Audited-safe (not changed): theme-color/banner inline styles (React CSSOM).

### DEFERRED — full script-src CSP (report-only → enforce)
Ship `Content-Security-Policy-Report-Only` first for 1–2 weeks, watch Sentry, then enforce with nonces. Draft enforce policy from the audit:
`default-src 'self'; script-src 'self' 'nonce-XXX' https://www.googletagmanager.com https://connect.facebook.net https://embed.tawk.to https://*.tawk.to https://js.stripe.com https://maps.googleapis.com https://maps.gstatic.com https://assets.calendly.com; style-src 'self' 'unsafe-inline' https://assets.calendly.com https://embed.tawk.to; img-src 'self' data: blob: https:; connect-src 'self' https://*.tawk.to https://region1.google-analytics.com https://www.google-analytics.com https://api.stripe.com https://maps.googleapis.com https://*.ingest.sentry.io https://*.ingest.us.sentry.io; frame-src 'self' https://js.stripe.com https://*.tawk.to https://calendly.com; font-src 'self' data: https://embed.tawk.to; worker-src 'self' blob:`
Keep 'unsafe-inline' for style-src only (theme colors via countless inline style={{}}); NEVER for script-src. Nonce path: generate per-request in src/proxy.ts, thread into root layout; next/script GA/FB inline + the site-page sticky-CTA inline `<script>` need the nonce manually (won't auto-nonce). HSTS: add `preload` + submit to hstspreload.org only after a week of no plain-HTTP subdomain need.
### Optional defense-in-depth (not shipped): validate primaryColor hex + bannerUrl http(s) on write (cosmetic-corruption only, not XSS).

## MARKETING CONSENT — DECISION: LEAVE PRE-TICKED (Luigi 2026-07-06)
Luigi reviewed the CASL/GDPR pre-ticked-consent flag (LAUNCH-READINESS High) and chose to LEAVE the marketing-consent box pre-ticked (opt-out), per the existing deliberate note at OrderingPageClient.tsx:1405-1420. Do NOT flip it or re-raise unless a complaint lands or Luigi asks. The code already has the flip instructions if needed.

## i18n RECEIPT ITEM HEADERS — DONE (2026-07-06). FOLLOW-UP: customer SMS still English.
EmailParts OrderItemsTable Qty/Items/Price/Note now localized (receipt.customer.qty/items/price/lineNote x38); customer OrderConfirmation passes them. OrderTotals was already localized. Kitchen/staff email stays English by design.
OPEN: `buildCustomerSms` (src/lib/notifications.ts:52) builds English literals for orderConfirmed/orderStatusUpdate(accepted/ready/completed/rejected/missed)/orderDelayed/reservationConfirmation. To localize: thread the resolved customer `locale`+dict into buildCustomerSms (currently only takes restaurantName+payload+hoursFormat), add sms.* ICU keys x38 (mind {orderNumber},{eta},{restaurantName} placeholders), run parity. Bigger job; deferred.

## FABRIZIO REPORTS BATCH (2026-07-06 evening) — 3 reports
1. **cmr80joh0 — PIN-TO-TOP CATEGORIES** (feature, Luigi: go ahead): extend the pin-to-top featured strip to whole CATEGORIES (not just dishes). A pinned category shows in the strip; tapping it scrolls/redirects to that category's row in the menu.
2. **cmr803ovq — CATEGORY LABELS/LOG IN DEPTH** (Luigi: PLAN FIRST, then fix + test): (a) category-level service-restriction + availability-window labels next to the category NAME (customer side — verify mobile layout; screenshots show partial working); (b) BACKEND labels in the menu editor next to a category noting it/some dishes have active exceptions (restriction/availability); (c) a CHANGE LOG of backend menu edits by the owner (who/what/when — audit trail); (d) category AVAILABILITY time-slot display (appears partly done).
3. **cmr80t9rk — 2 BUGS** (Luigi: fix in depth): (a) a dish set SOLD OUT in the main menu is still SELECTABLE inside a bundle/combo/promo picker — customer only finds out at order placement (server rejects, but the picker UI doesn't disable it); (b) PROMO PRECEDENCE: an EXCLUSIVE promo should block all other promos while it applies, but a STANDARD 20% still stacks. Need STANDARD / EXCLUSIVE / MASTER precedence working (GloriaFood parity: exclusive blocks simultaneous promos). Money-adjacent — preview==charge + all-promo-types rule.

## GLORIAFOOD PARITY GAP — BOGO "extra charges" option (Luigi flagged 2026-07-07, during promo sweep)
GloriaFood's BOGO/BuyOneGetOne config has a dropdown FeeFree lacks: **No extra charges** (whole free item incl. toppings/size is free — FeeFree's current + only behavior) / **Charge extra for "Choices/Addons"** (base free, toppings still charged) / **Charge extra for "Choices/Addons" & "Sizes"** (base free, toppings + size upgrades charged). FeeFree matches only the DEFAULT ("No extra charges"). To add the other two: (1) cart must send each line's BASE price vs addon/size upcharge breakdown (data-flow change — PizzaBuilder + apply-promos + orders route), (2) engine frees base-only when mode≠none (bogoResult/buyNGetFreeResult — currently frees full unit.price incl. toppings), (3) admin BOGO config field `freeItemExtraChargeMode`, (4) i18n ×38 for the option labels. Real feature, medium effort. For the parity test now, use GloriaFood "No extra charges" to compare apples-to-apples.
